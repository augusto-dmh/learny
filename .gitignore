.agents/.skill-lock.json.backup

# OMC orchestration state (local tooling, not project artifacts)
.omc/
**/.omc/

# Production secret env files injected into the prod compose overlay (never
# committed — created from the *.env.production.example templates, AD-042/AD-043).
secrets/
*.env.production

# Local silver eval tier — copyrighted real-book cases, snippets, and results
# live only on the maintainer's machine; only the runner/loader code is committed.
# The golden tier under evals/results/ stays tracked.
evals/silver/
