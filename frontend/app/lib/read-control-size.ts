/** Minimum CSS size for /read controls added or relocated this cycle. */
export const READ_CONTROL_MIN_PX = 44;

export const readControlMinStyle = {
  minWidth: READ_CONTROL_MIN_PX,
  minHeight: READ_CONTROL_MIN_PX,
} as const;
