# STATE — Project Memory

Persistent decision log, blockers, and handoff for tlc-spec-driven cycles.
Architecture decisions live in `docs/adr/` and `docs/tdd/`; this file references them, never duplicates them.

## Decisions Log

Accepted architecture (locked — sourced from ADRs/TDD, not re-decided here):

| ID | Decision | Source |
|---|---|---|
| AD-001 | Workflow model: TDD-001's 10-phase plan is the roadmap; tlc runs one cycle per slice; ADRs/TDD are locked constraints, not re-opened in Design. | this session |
| AD-002 | First cycle scope = scaffold + full identity (TDD-001 Implementation Plan Phases 1+2). | this session |
| AD-003 | Open questions #1/#2/#3 (lib, CSRF, object storage) resolved inside tlc Discuss → recorded as AD-006..AD-008 below. | this session |
| AD-004 | Stack: Python/FastAPI, React/Next.js, PostgreSQL+pgvector, Redis/Celery, S3-compatible storage. | ADR-004, ADR-006, ADR-014, ADR-013 |
| AD-005 | Backend-owned auth, HTTP-only cookies; thin same-origin Next.js proxy; FastAPI authoritative for authz. | ADR-012, ADR-015, ADR-017 |
| AD-006 | Argon2id hashing (pwdlib/argon2-cffi) + opaque server-side session tokens in PostgreSQL. | Discuss → context.md (TDD OQ #1) |
| AD-007 | SameSite=Lax+Secure+HttpOnly cookie + Origin check + session-bound (synchronizer) CSRF token. | Discuss → context.md (TDD OQ #2) |
| AD-008 | Self-hosted MinIO in Docker Compose for local and first VPS; swappable via storage port. | Discuss → context.md (TDD OQ #3) |
| AD-009 | Source upload transport = direct multipart through FastAPI (bytes stream through the API to storage). Presigned direct-to-storage upload deferred. | ADR-018 / Cycle 2 context.md (D-1) |
| AD-010 | Cycles ship as full vertical slices (backend + frontend) per feature, matching Cycle 1 cadence. | Cycle 2 context.md (D-2) |
| AD-011 | S3-compatible storage adapter uses boto3 (S3 API) behind `StoragePort`, keeping MinIO/S3/R2 swappable. | Cycle 2 context.md (D-3) |
| AD-012 | Cycle 3 `worker-foundation` (TDD Phase 4) ships the ingestion job engine only — Celery task drives the full `queued→running→succeeded/failed` lifecycle with a **stub body** (`# TODO(Phase 5): parse EPUB`, no parsing). Real parsing stays in Phase 5; the two are separate PRs. | Cycle 3 Specify (user: "Just 4") |
| AD-013 | Ingestion trigger = explicit `POST /api/sources/{id}/ingestion` (start/restart) only; Cycle 2 upload flow unchanged; `source.status` stays `uploaded` until started. No auto-enqueue on upload. | Cycle 3 Specify |
| AD-014 | Durable job state = `ingestion_jobs` + append-only `ingestion_events` (both this cycle); ownership reachable only via parent `source`; queue messages carry ids only (`source_id`, `ingestion_job_id`). `source.status` is a projection: `uploaded→processing→ready/failed`. | Cycle 3 Specify |
| AD-015 | Cycle 3 includes a minimal frontend slice (AD-010): ingestion status badge + "Start ingestion" control on the existing sources screen via the same-origin proxy. No polling/auto-refresh this cycle. | Cycle 3 Specify |
| AD-016 | Worker-triggering commands commit durable job state first, then enqueue **after commit** through a Learny `IngestionEnqueuer` port, compensating to terminal `failed` if enqueue fails; Celery/`apply_async` never enters application or domain code. Future worker-triggered features (embedding, indexing, evaluation) follow this. Active-job uniqueness is a partial unique index; the Phase-5 parse seam is an injectable `IngestionStep` port (no-op stub this cycle). | Cycle 3 Design |
| AD-017 | EPUB parsing uses **ebooklib** behind a Learny `EpubParserPort` (edge adapter in `infrastructure/ingestion/`); Docling deferred as a possible second adapter when PDF arrives. Resolves CLAUDE.md's open parsing-library choice. | Cycle 4 Discuss (D-1) |
| AD-018 | Canonical corpus schema: `corpus_documents` (UNIQUE `source_id`) → `corpus_sections` (TOC-derived `section_path`, `anchor`, derived `markdown`) → `corpus_blocks` (preserved HTML) + `corpus_chunks` (structure-first, ≤ `LEARNY_CHUNK_MAX_CHARS`, nullable `page_span`). Ownership via parent `source` only. Re-ingestion = atomic replace (delete+cascade+insert) inside the single step transaction; no corpus versioning without a future ADR. | Cycle 4 Specify/Design (D-3/D-4) |
| AD-019 | Cycle 5 `retrieval-indexes` (TDD Phase 6) defers the concrete cloud embedding provider/model: ship a Learny `EmbeddingPort` + a **deterministic, dependency-free local adapter** as the default; the production provider is its own follow-up ADR. Keeps retrieval provider-agnostic + testable without a network/secret, consistent with CLAUDE.md ("no provider default without an ADR") and ADR-0007. | Cycle 5 context.md (D-1) |
| AD-020 | Retrieval columns live on `corpus_chunks`: nullable `embedding vector(1536)` (cosine `vector_cosine_ops`, HNSW `m=16,ef_construction=64`) + `STORED` generated `search_vector tsvector` (section title `section_path->>-1` weighted `'A'` over `text` `'D'`, GIN). `pgvector>=0.3,<0.5` added; `vector` extension created in the migration. Dim `1536` is provider-neutral; a different-dim model later is a dim-change migration + re-embed (embeddings re-indexable, ADR-0001). | Cycle 5 context.md (D-2/D-3/D-4) |
| AD-021 | Embedding runs as an **embed+index step inside `run_ingestion`**, in its own committed transaction, after the corpus-build commit and before terminal success — keeping the provider call out of the corpus-write transaction; reuses the existing `IngestionStep` retry/terminal classification. No separate `embed.run` task this cycle. Re-ingestion re-embeds. | Cycle 5 context.md (D-5) |
| AD-022 | Retrieval = single-statement hybrid **RRF** query over `corpus_chunks` **scoped to one `source_id`**, two ranked CTEs (`Σ 1/(k+rank)`), semantic arm skips NULL embeddings, behind a `RetrievalPort`, projecting a frozen `Evidence` result (`chunk_id, source_id, section_path, anchor, page_span, snippet, score`). Tuning (`semantic_limit`, `lexical_limit`, `rrf_k=60`, `top_k`, `max_top_k`, `hnsw_ef_search`, `embedding_dim`) in `LEARNY_` settings. No reranker (ADR-0006). | Cycle 5 context.md (D-6) |
| AD-023 | Cycle 5 vertical slice = backend capability **+ a thin owner-scoped `POST /api/sources/{id}/retrieve`** returning evidence; **no frontend** this cycle. Raw hybrid results are not an MVP user surface (Phase 7 cited Q&A / Phase 8 teaching are); empty match → **200 empty list** (the Phase-7 "not found in source" hook); missing/non-owned → 404. Partial, deliberate departure from AD-010's full-slice cadence — flagged at the merge gate. | Cycle 5 context.md (D-7/D-8) |
| AD-024 | Cycle 6 `cited-qa` (TDD Phase 7) ships `AnswerGenerationPort` + a **deterministic, evidence-grounded local adapter** (`local-extractive`) as the default; the cloud LLM provider adapter (OpenAI/Anthropic) is deferred to its own ADR (mirrors AD-019; CLAUDE.md forbids a provider default without one). Port contract, endpoint, grounding, and "not found" semantics are adapter-independent. Flag at merge gate: the provider ADR is the blocking follow-up for LLM prose. | Cycle 6 context.md (D-1) |
| AD-025 | Q&A is **stateless** this cycle: no `qa_answers`/`answer_citations` tables (TDD-001 marks them optional; no MVP consumer). Persistence needs a follow-up decision when a consumer (history UI, evaluation replay) exists. | Cycle 6 context.md (D-2) |
| AD-026 | Cited Q&A contract: `POST /api/sources/{source_id}/questions`, body `{question}` (trimmed, 1..`LEARNY_QA_QUESTION_MAX_CHARS`); 200 always carries `answer_status ∈ {answered, not_found_in_source}` + `retrieval` diagnostics + `model`; 404 missing/non-owned (no disclosure), 409 not-ready, 422 bounds, 429 throttle, 502 generation failure. "Not found in source" is a product outcome, never an HTTP error. No `top_k` in the public API. | Cycle 6 context.md (D-3) |
| AD-027 | Citation grounding is enforced in the **application service**, not adapters: cited chunk ids outside the retrieved evidence set are discarded (evidence-rank order, deduped); none left or empty/`found=false` result ⇒ `not_found_in_source`; empty evidence short-circuits without invoking the port. Applies automatically to every future provider adapter. | Cycle 6 context.md (D-4) |
| AD-028 | Cycle 6 restores AD-010's full vertical slice: browser Ask panel (`/sources/{id}/ask` + link from the sources list for ready sources) through the same-origin proxy, rendering answer + citations, explicit not-found, and readable error states. | Cycle 6 context.md (D-5) |
| AD-029 | Questions endpoint reuses the existing swappable in-process rate limiter (`rate_limit_questions`, same proxy-IP KNOWN LIMITATION); question bound `LEARNY_QA_QUESTION_MAX_CHARS=2000`; evidence depth `LEARNY_QA_EVIDENCE_TOP_K=8` (server-side only). | Cycle 6 context.md (D-6) |
| AD-030 | Cycle 7 `teaching-sessions` (TDD Phase 8): session target = one corpus section by stable `anchor` (chapters are depth-0 sections); target snapshot (`anchor`,`section_path`,`title`) stored on the session. Passage/chunk targets and whole-source sessions deferred. | Cycle 7 context.md (D-1) |
| AD-031 | Turn retrieval is scoped to the target subtree: service resolves target+descendant anchors from the structure read model; `RetrievalPort.search` gains an optional `anchors` filter applied in both hybrid CTE arms (`anchor = ANY`). No post-filtering, no second port. | Cycle 7 context.md (D-2) |
| AD-032 | New `TeachingGenerationPort` (mirrors AD-024): `model: str` + `generate(message, target_section_path, history, evidence)`; bounded `history` DTO in the contract from day one; deterministic `local-extractive` default adapter; cloud LLM adapter stays blocked on the provider ADR. | Cycle 7 context.md (D-3) |
| AD-033 | Teaching persistence: `teaching_sessions` → `teaching_turns` (UNIQUE(session_id,turn_index); one row = message+response) → `teaching_turn_citations` with `chunk_id` as plain UUID (no FK) + denormalized anchors/snippet so history survives corpus replace (AD-018). Ownership via parent source (AD-014). No session status column this cycle. | Cycle 7 context.md (D-4) |
| AD-034 | Turn contract mirrors AD-026/027: sync in-request generation; 201 `answer_status ∈ {answered, not_found_in_source}`; grounding + empty-evidence short-circuit in the application service; 404/409(not-ready|stale-target|index-race)/422/429/502; 502 persists nothing; not_found turns persist; bounded context = last `LEARNY_TEACHING_HISTORY_TURNS` turns. | Cycle 7 context.md (D-5) |
| AD-035 | API = TDD trio + additive owner-scoped `GET /api/sources/{id}/teaching-sessions` (list w/ `turn_count`); rate limit both POSTs via the existing in-process limiter. Frontend slice = `/sources/{id}/teach` screen: target picker from CORP-11 structure, previous-sessions resume, conversation view with citations/not-found/error states. Settings: `LEARNY_TEACHING_MESSAGE_MAX_CHARS=2000`, `LEARNY_TEACHING_EVIDENCE_TOP_K=8`, `LEARNY_TEACHING_HISTORY_TURNS=6`. | Cycle 7 context.md (D-6/D-7) |
| AD-036 | Cycle 8 `golden-fixtures` (TDD Phase 9) realizes evaluation as a versioned, deterministic golden **test harness** under `backend/tests/` — fixtures + expected values + fixture-driven checks. No `evaluation_fixtures/evaluation_runs/evaluation_results` SQL tables, service, Celery task, or endpoint: ADR-016 defers the dashboard/metric scoring; nothing consumes persisted runs (AD-025 stateless precedent). Persistence is a follow-up when a real consumer exists. | Cycle 8 context.md (D-1) |
| AD-037 | Golden fixture EPUBs are **authored synthetic** (built as reviewable code): reuse `tests/fixtures_epub.py` builders for ingestion structure + one new topically-rich `golden_corpus.py` book with lexically-disjoint per-section prose for retrieval targets. No third-party/real EPUB binary. Resolves TDD open question #9 (fixture licensing) by avoiding third-party text entirely. | Cycle 8 context.md (D-2) |
| AD-038 | Golden expectations use **stable identity** (`anchor`, `section_path`, snippet text), never per-run chunk UUIDs (AD-018). Ingestion golden runs **pure** (real parser + markup + `BuildCorpus` over `FakeCorpusRepository`, no DB); retrieval + citation golden run **integration** against the pgvector test DB (real corpus repo + deterministic embed + real hybrid query), guarded by `requires_db` (skip when `LEARNY_TEST_DATABASE_URL` unset). Matches the repo's marker-based unit/integration split. | Cycle 8 context.md (D-3) |
| AD-039 | Cycle 8 is a **backend/test-only** slice — no frontend, no schema/migration, no endpoint. A CI/dev regression harness has no user surface. Deliberate flagged departure from AD-010's full-slice cadence (precedent AD-023); surfaced at the merge gate. | Cycle 8 context.md (D-4) |
| AD-040 | Cycle 9 `production-readiness` (TDD Phase 10) ships three **provider-neutral** slices — backend observability hooks + a production Docker Compose **overlay** + operator runbooks (backup/restore, rollback) — and locks **no** monitoring/metrics/TLS/reverse-proxy/VPS vendor. TDD open question #10 stays open project-wide as the flagged blocking follow-up. Honors ADR-0008 §5 (defer observability/TLS/proxy/backup impl) + CLAUDE.md (monitoring stack reserved for its own ADR). | Cycle 9 context.md (D-1) |
| AD-041 | Observability = request-ID + trace-context middleware + structured JSON logging (env-toggled `LEARNY_LOG_FORMAT`, human default) carrying the TDD required trace fields, + one per-request access log with latency/outcome, + worker lifecycle duration. **No** metrics scrape endpoint and **no** new observability dependency this cycle. Correlated latency-bearing structured logs are the substrate every TDD metric derives from and stay provider-neutral; a Prometheus/OTel endpoint would lock what ADR-0008 §5 / OQ #10 defer. Existing NFR-SEC-004 redaction preserved through the JSON path. | Cycle 9 context.md (D-2) |
| AD-042 | Deployment shape = `docker-compose.prod.yml` **overlay** applied via `-f docker-compose.yml -f docker-compose.prod.yml`, overriding only prod-specific keys — keeps local aligned (ADR-0008 §3), a standalone prod compose was rejected (duplicates topology, drifts). Hardening: no host ports for `db`/`redis`/`minio`; `restart: unless-stopped`; pinned image tags (no `:latest`); secrets via `env_file`/env injection; `LEARNY_SESSION_COOKIE_SECURE=true`+`LEARNY_ENVIRONMENT=production`+`LEARNY_LOG_FORMAT=json`; production run commands (uvicorn multi-worker; Next.js build+start via a production Dockerfile target). Reverse proxy + TLS documented as an external front, not embedded (OQ #10). | Cycle 9 context.md (D-3) |
| AD-043 | Ops runbooks live under `docs/ops/` — `backups.md` (PostgreSQL logical dump+restore, object-storage bucket mirror, restore drill, retention as operator TODO) and `rollback.md` (independent `api`/`worker`/`web` image revert, migration reversibility + forward-only exception, the TDD operational-rollback-triggers table, corpus atomic-replace/no-versioning implication per AD-018). Provider-neutral commands; verified by presence/content tests, not live execution. | Cycle 9 context.md (D-4) |
| AD-044 | Cycle 9 is a **backend + config + docs** slice — a production frontend build target is added but no UI behavior changes; no schema/migration. Deliberate flagged departure from AD-010's full-slice cadence (precedent AD-023, AD-039); surfaced at the merge gate. Production readiness has no end-user surface. | Cycle 9 context.md (D-5) |
| AD-045 | v2 roadmap driver: accepted RFC-002 (cycles A–G) replaces TDD-001 as the roadmap source; ROADMAP.md maps its cycles; per-cycle design happens inside each tlc cycle. | Cycle A context.md (D-1) |
| AD-046 | Pre-authored, user-approved artifacts (README, QA docs, research, RFC-002) land unmodified as Cycle A's opening docs commits. | Cycle A context.md (D-2) |
| AD-047 | CI = 4-job GitHub Actions workflow per the 2026-07-12 oss-maturity research, adapted to LEARNY_* env names and /healthz; lint gate is `ruff check` only (format not enforced — 38 files not format-clean, reformat out of scope). | Cycle A context.md (D-3) |
| AD-048 | License: Apache-2.0 (patent grant, enterprise-readable, sole-author relicensing preserved). | Cycle A context.md (D-4) |
| AD-049 | F4 root cause: migrations/env.py clobbered caller-set sqlalchemy.url with the settings URL, so the test bootstrap migrated the dev DB and a fresh test DB stayed schemaless until test_migrations ran. Fix: env.py honors a caller-provided URL, settings only as fallback. | Cycle A context.md (D-5) |
| AD-050 | Cycle A ships docs/fixes/CI/hygiene with no frontend product feature — deliberate departure from AD-010 (precedent AD-023/AD-039/AD-044). | Cycle A context.md (D-6) |
| AD-051 | Cycle B `v2-embeddings` (RFC-002 Cycle B) = backend-only slice extending the Cycle-5 substrate (EmbeddingPort, deterministic adapter, hybrid RRF, Celery ingestion), not a rebuild. Fourth consecutive backend-only slice — deliberate departure from AD-010 (precedent AD-023/AD-039/AD-044/AD-050). | Cycle B context.md (D-1) |
| AD-052 | `OpenAIEmbeddingAdapter` behind `EmbeddingPort`: `text-embedding-3-large` + request `dimensions=1536` (fits `vector(1536)`; HNSW 2000-dim limit rules out native 3072). Provider selected by `LEARNY_EMBEDDING_PROVIDER` (default `local` → deterministic; `openai`); deterministic stays the CI/test default so the suite runs offline/key-free. Adapter sub-batches ≤2048 inputs/≤250k tokens, no client-side retry (Celery owns retries). First paid provider SDK — this is the cycle CLAUDE.md gates the OpenAI SDK to; ratified by ADR-0019. Voyage-4 rejected (1024/2048 dims → forces a vector-column migration) but recorded as the ADR alternative. | Cycle B context.md (D-2) |
| AD-053 | Per-chunk model versioning: nullable `embedding_model text` on `corpus_chunks` = `"<model>@<dims>"` (`local-deterministic@1536` / `text-embedding-3-large@1536`); `EmbeddingPort` gains a stable `model: str` (mirrors `AnswerGenerationPort.model`); `EmbedCorpus` + reembed write it. Per-chunk (not per-document) so mid-reembed mixed-model states and eval-snapshot pinning are expressible. | Cycle B context.md (D-3) |
| AD-054 | Language-aware FTS (fixes F8): add `search_config text NOT NULL DEFAULT 'simple'` (resolved Postgres regconfig) on `corpus_chunks`; replace the hardcoded-`english` GENERATED `search_vector` with a plain `tsvector` maintained by a `BEFORE INSERT/UPDATE` trigger keyed on the row's `search_config` (a STORED generated column can't use a per-row regconfig — not IMMUTABLE); GIN rebuilt. Pure `resolve_text_search_config(dc:language)->regconfig` (fallback `simple`). Lexical retrieval arm uses `websearch_to_tsquery(search_config::regconfig, :q)` from each chunk's own trusted column — no new `RetrievalPort` param, no app-level query-time language lookup, no untrusted interpolation; semantic arm unchanged. | Cycle B context.md (D-4) |
| AD-055 | `reembed_document(source_id)` Celery task: idempotent/resumable — selects `embedding IS NULL OR embedding_model IS DISTINCT FROM :target`, commits per batch; HNSW drop-before/rebuild-after (better graph + faster bulk than incremental); provider from settings (deterministic default → reembed runs offline in CI); reuses `embedding_batch_size` for the loop (OpenAI adapter sub-batches to API limits). No HTTP endpoint — ops-invoked (backend-only slice). | Cycle B context.md (D-5) |
| AD-056 | Tier-2 retrieval eval: 30–60 hand-labeled `(query→anchor)` pairs over the golden book; recall@1/@5 + MRR over real hybrid retrieval + deterministic adapter; snapshot records model+dims + metrics with a fixed-threshold regression gate; `@pytest.mark.live` OpenAI variant skipped without a key. Committed snapshot is `local-deterministic@1536` (CI offline); the real `text-embedding-3-large@1536` snapshot is a keyed follow-up (consistent with AD-036: evaluation as a deterministic harness). | Cycle B context.md (D-6) |
| AD-057 | ADR-0019 ratifies OpenAI `text-embedding-3-large@1536` Accepted (per-chunk model versioning, deterministic adapter retained as default, Voyage-4 recorded alternative) — makes the embedding provider binding per CLAUDE.md. Closes the embedding half of the AD-019/AD-024 provider follow-up; the cloud **generation** provider (Claude, Cycle C) stays open. | Cycle B context.md (D-7) |
| AD-058 | Cycle C generation model: Anthropic `claude-sonnet-4-6` for answers + teaching via `LEARNY_GENERATION_MODEL` (config-swappable); judge model `claude-haiku-4-5` via `LEARNY_JUDGE_MODEL`. Sonnet 5 (tokenizer re-baseline) and Opus 4.8 (cost) recorded alternatives; ADR-0020 ratifies. Closes the generation half of the AD-024/AD-032 provider follow-up. | Cycle C context.md (D-1) |
| AD-059 | Single `LEARNY_GENERATION_PROVIDER` (`local` default \| `anthropic`) for both generation ports; `build_answer_adapter`/`build_teaching_adapter` factories mirror AD-052's embeddings pattern (unknown → ValueError; empty `LEARNY_ANTHROPIC_API_KEY` with `anthropic` → fail-fast at construction). Deterministic adapters remain the CI/offline default; `anthropic` SDK gated to this cycle per CLAUDE.md. | Cycle C context.md (D-2) |
| AD-060 | Citations contract: one plain-text citations-enabled document per evidence chunk (evidence order); `document_index → chunk_id` is the only mapping (title/context never parsed). Relevance-aware not-found (F5): frozen system prompt + exact whole-reply sentinel `NOT_FOUND_IN_SOURCE` → `found=False`; AD-027 grounding remains the backstop (zero-citation prose → not_found). SDK transport retries only (no app retry layer); citations + structured outputs never combined in one request (API 400). | Cycle C context.md (D-3, D-6) |
| AD-061 | Streaming: Learny-owned domain stream events (`AnswerTextDelta`/`AnswerCompleted`) + `generate_stream` on both generation ports; application-layer sentinel hold-back; grounded citations emitted once in a terminal `data-citations` part; UI Message Stream v1 lives only in the edge presenter (`ui_message_stream.py`); `/stream` sibling endpoints reuse the JSON endpoints' dependency lists; teaching turn persists only on stream completion; FastAPI floor raised to `>=0.135` for `fastapi.sse`. | Cycle C context.md (D-4) |
| AD-062 | Generation eval: port-level replay snapshots with a `--record-generation` flag (snapshot-dependent tests skip when none committed — CI offline, AD-056 precedent); exact citation-validity invariants run every PR against the deterministic adapter + golden book and any committed snapshots; `@pytest.mark.live` smoke per adapter. No VCR/HTTP cassettes; no Ragas (locked v2 decision). | Cycle C context.md (D-5) |
| AD-063 | Judge harness: Learny-owned module (`app/eval/judge.py`) scoring faithfulness (claim-support ratio) + relevancy (1–5) with structured outputs on the judge model; versioned prompt files; JSONL results in `evals/results/`; nightly `eval.yml` (cron + dispatch, secret-gated, `LEARNY_EVAL_MAX_CASES` cost cap). Thresholds calibrate first (`LEARNY_EVAL_GATE` off), gate after baselines; judge is nightly signal, never a PR gate. | Cycle C context.md (D-5) |
| AD-064 | Cycle C `v2-generation` ships backend-only (5th consecutive; SSE consumer is Cycle D by design) — deliberate departure from AD-010 (precedent AD-023/AD-039/AD-044/AD-050/AD-051). Flag at merge gate. | Cycle C context.md (D-7) |
| AD-065 | Cycle D `v2-frontend` (RFC-002 Cycle D) rebuilds the UI layer (pages/components/global styles) on Tailwind v4 + shadcn/ui + AI Elements (vendored source); keeps + extends the tested transport layer (`app/lib/*` clients, proxy, 13 test files). Restores AD-010's full vertical slice. | Cycle D context.md (D-1) |
| AD-066 | Streaming client = AI SDK `useChat` + `DefaultChatTransport` against the existing Cycle-C UI Message Stream v1 endpoints through the same-origin proxy; `prepareSendMessagesRequest` maps to Learny contracts (`{question}`/`{message}`, latest message only, `X-CSRF-Token`); transport config isolated in one module (client mirror of `ui_message_stream.py`). | Cycle D context.md (D-2) |
| AD-067 | New owner-scoped read endpoint `GET /api/sources/{source_id}/section?anchor=` → `{anchor,title,section_path,markdown}`; 404 missing/non-owner/no-corpus/unknown-anchor (mirrors structure endpoint, no disclosure); anchor as query param (`href[#fragment]` format). The only backend surface added this cycle. | Cycle D context.md (D-3) |
| AD-068 | Reader = `/sources/[id]/read?anchor=` rendering one section's markdown, scroll + transient section-level highlight; citation popover "Open in book" navigates there. Snippet-level text highlight deferred. | Cycle D context.md (D-4) |
| AD-069 | App shell = shadcn `Sidebar` (library w/ status badges + collapsible per-book section tree) + header (email, account, logout, theme toggle); `next-themes` class-strategy dark mode, system default; auth screens unshelled. | Cycle D context.md (D-5) |
| AD-070 | Ingestion progress = new frontend client for existing `GET /api/sources/{id}/ingestion`; fixed 3 s per-processing-source polling, stop on terminal/unmount; failed shows latest event message + existing restart control. No push transport. | Cycle D context.md (D-6) |
| AD-071 | Cycle D testing keeps repo conventions (fetchImpl-injected client tests; jsdom `routedFetch` component tests); streaming tested via `ReadableStream` SSE fixtures through stubbed fetch; vendored shadcn/AI Elements source exercised through compositions, never unit-tested itself. Gates unchanged. | Cycle D context.md (D-7) |
| AD-072 | Cycle E `v2-active-recall` (RFC-002 Cycle E) ships the full flagship slice in one cycle: schema (4 tables), QuizGenerationPort + SchedulingPort with adapters, Celery deck pipeline, review endpoints, review UI, genanki `.apkg` export, PR + nightly evals, ADR-0021. New edge deps `fsrs>=6,<7` + `genanki` (both MIT, deterministic, network-free; ADR-0009 "specialized libraries at edges"). Restores AD-010 full-slice cadence. | Cycle E context.md (D-1) |
| AD-073 | Quiz schema = content/scheduling/history split: `quiz_items` (content + citation snapshot anchor/section_path/source_excerpt/chunk_hash + `content_key` + status active\|stale\|orphaned) / `quiz_item_scheduling` (1:1, real FSRS columns, indexed `due`) / append-only `review_log` / `quiz_generation_jobs` (mirrors ingestion_jobs). Ownership via `source_id` only (AD-014); **no FK to corpus_chunks** (ids regenerate on re-ingest — snapshot pattern per AD-033); upsert on `(source_id, content_key)` preserves scheduling; no distractor columns (locked v2 decision). | Cycle E context.md (D-2) |
| AD-074 | Quiz generation: `LEARNY_GENERATION_PROVIDER` selects deterministic `local` (CI default) vs Anthropic on `LEARNY_QUIZ_MODEL` (default `claude-haiku-4-5`); structured outputs per the judge.py pattern (citations API mutually exclusive) with `source_chunk_id` enum restricted to section chunk ids + required verbatim `anchor_quote`; deterministic server-side QC: whitespace-normalized quote containment, cloze-mask-in-quote, embedding dedup ≥0.90 via EmbeddingPort, `content_key` exact dedup; discard never repair. Types: `free_recall` + single-mask `cloze` only. LLM critique pass deferred (nightly judge is the quality sensor). | Cycle E context.md (D-3) |
| AD-075 | Deck pipeline: port shape `begin_deck(sections)→DeckHandle` / `collect_deck(handle)→pending\|results`; local adapter synchronous, Anthropic adapter = Message Batches API (one request per eligible section, ~50% cost); Celery `generate_quiz_deck` + self-rescheduling poll task bounded by `LEARNY_QUIZ_BATCH_TIMEOUT_S` (3600); one in-flight job per source (409); per-section failures counted, not fatal. | Cycle E context.md (D-4) |
| AD-076 | `SchedulingPort` + py-fsrs adapter: FSRS-6 defaults (`desired_retention=0.9`, default learning steps, `maximum_interval=36500`), fuzzing via `LEARNY_FSRS_FUZZING` (on in prod, off in tests); ratings 1–4 ↔ Again/Hard/Good/Easy 1:1; new items due-now; review atomically updates scheduling + appends log; no idempotency key (A-2); early review allowed (A-4). Optimizer deferred. | Cycle E context.md (D-5) |
| AD-077 | Quiz API: `POST /api/sources/{id}/quiz/deck` (202), `GET /api/sources/{id}/quiz` (items + job state + counts, deck polling target), `GET /api/reviews/due` (cross-source w/ optional `source_id` filter, limit 20/max 100, `due ASC, id ASC`), `POST /api/quiz-items/{id}/reviews` (rating 1–4; 422 out of range; 409 non-active), `GET /api/sources/{id}/quiz/export` (.apkg). Ownership via parent source, 404 non-disclosure, origin+CSRF+`rate_limit_quiz` on state-changing routes. Cross-source due queue = interleaving for free. | Cycle E context.md (D-6) |
| AD-078 | Re-ingest reconciliation runs inside `run_ingestion` after corpus replace: anchor ∧ excerpt → keep; anchor ∧ ¬excerpt → `stale`; ¬anchor ∧ excerpt found elsewhere → relocate (stay active); else `orphaned`. Scheduling + review_log never modified/deleted (FSRS state describes the user's memory, not the document). Stale/orphaned excluded from due queue, still listed. | Cycle E context.md (D-7) |
| AD-079 | Review UI: global `/review` route + per-source entries; Anki-shaped card flow (question → Reveal → answer + citation footnote w/ "Open in book" via existing reader, snapshot + "source changed" fallback → 4-button grade → advance → session summary); library "Generate quiz deck" with AD-070 3s polling against `GET .../quiz`; plain fetch `lib/quiz.ts` (no streaming); AD-071 test conventions. | Cycle E context.md (D-8) |
| AD-080 | Quiz evals: deterministic PR gate over golden book + local adapter (100% groundedness containment, cloze mask validity, anchor resolvability — replaces dropped distractor checks); nightly answerability round-trip judge (`live and eval`, structured outputs on `LEARNY_JUDGE_MODEL`, versioned prompt, JSONL results) via existing `eval.yml`; judge stays nightly-signal-never-PR-gate (AD-063). | Cycle E context.md (D-9) |
| AD-081 | Anki export: genanki `.apkg`, note GUID = stable digest of (source_id, content_key) so re-import updates in place; Basic model for free_recall, Cloze model for cloze; citation footnote on every note. CSV rejected (duplicates on re-import, loses cloze typing). | Cycle E context.md (D-10) |
| AD-082 | Cycle F `v2-ingestion-breadth` (RFC-002 Cycle F) ships backend+worker+compose, no frontend feature: upload UI has no accept filter and all screens consume format-agnostic API shapes, so PDF appears without UI work. 4th deliberate AD-010 departure (precedent AD-023/AD-039/AD-044) — flag at merge gate. | Cycle F context.md (D-1) |
| AD-083 | Parser port renamed `EpubParserPort` → `DocumentParserPort` (same `parse(source_bytes, *, filename) -> ParsedBook` shape); `InvalidEpubError` → `InvalidDocumentError`; format-dispatch factory at the worker composition root selects the adapter by `source.content_type`. ebooklib stays the EPUB adapter (Docling EPUB backend strips anchors — research + docling #2929). | Cycle F context.md (D-2) |
| AD-084 | Format-agnostic corpus normalization = pure/deterministic/idempotent `normalize_book(ParsedBook) -> NormalizationResult` in the application layer, applied inside `BuildCorpus` between parse and record building for every format. Heuristics (research §4 / Calibre family): title-inference cascade over generic `part0034`-style titles, flat-TOC hierarchy re-derivation from heading levels, depth clamp to parent+1, trivial-section merge (<30 words/no own heading/caption-only), Gutenberg START/END marker stripping. Emits a normalization counts job event. Fixes F7. | Cycle F context.md (D-3) |
| AD-085 | Anchor aliasing: merged-away section anchors persist in `corpus_sections.anchor_aliases TEXT[]` (migration 0009), written by corpus replace. Alias-aware consumers: `get_section` falls back to alias membership, quiz reconcile relocates alias-matched items to the canonical anchor, teaching-scoped retrieval expands target anchor to canonical+aliases. Canonical wins collisions. No GIN index yet (dozens of aliases max). | Cycle F context.md (D-4) |
| AD-086 | PDF anchor scheme: section anchor `pdf:{heading-path-slug}/b{ordinal:04d}-{sha256(section_text)[:16]}`; `page_span` persisted on chunks (reserved `SectionChunk.page_span`) and surfaced in section/citation reads; deterministic for identical bytes+parser version. Docling JSON pointers never used as anchors (index-fragile). EPUB chunks keep `page_span = None`. | Cycle F context.md (D-5) |
| AD-087 | PDF worker topology: `run_ingestion` routed at enqueue (`apply_async(queue="ingest-pdf")`) when source content type is PDF; all else stays default queue. New compose service `worker-pdf` (base+prod): `--queues ingest-pdf --concurrency 1 --max-tasks-per-child 1`, `mem_limit: 4g`. `docling` in uv optional extra `pdf`; dedicated Dockerfile target bakes models via `download_models()` at build (no network at task time); api/worker images unchanged. | Cycle F context.md (D-6) |
| AD-088 | PDF failure semantics: corrupt/encrypted/zero-text → `InvalidDocumentError` → terminal job failure, no retry (existing step classification). Resource kills contained by mem_limit + max-tasks-per-child=1; global `task_time_limit=1800` retained (covers ~400-page books at ~0.8–3 s/page). | Cycle F context.md (D-7) |
| AD-089 | Docling test strategy: adapter split into thin `_convert` (local docling import) + pure `DoclingDocument → ParsedBook` mapping; mapping unit-tested against constructed `docling-core` documents (dev dep); real-conversion tests generate a tiny PDF in-test and `pytest.importorskip("docling")` — skipped in CI, no binary fixtures committed. | Cycle F context.md (D-8) |
| AD-090 | GHCR images: exactly three — `ghcr.io/augusto-dmh/learny-backend` (runtime target, shared by api+worker), `learny-pdf-worker`, `learny-web` (prod target) — tagged `latest` + commit SHA, pushed with job-scoped `GITHUB_TOKEN` (`packages: write`, no PAT). Packages flipped public once after first push (OSS repo; VPS pulls credential-free). | Cycle G context.md (D-1) |
| AD-091 | Deploy workflow: separate `deploy.yml` triggered by `workflow_run` on CI success @ main (+ ref-guarded `workflow_dispatch`); 3 parallel build jobs → deploy job via `needs`; concurrency group `deploy` with `cancel-in-progress: false`; deploy job secret-gated (missing `VPS_*` → green exit + notice, eval.yml precedent). ci.yml stays untouched/`contents: read`. | Cycle G context.md (D-2) |
| AD-092 | Prod compose consumes GHCR: `docker-compose.prod.yml` adds `image: ghcr.io/...:${LEARNY_IMAGE_TAG:-latest}` on api/worker/worker-pdf/web while base keeps `build:` for dev/smoke; deploys pin `LEARNY_IMAGE_TAG=<sha>`; rollback = redeploy older sha (rollback.md updated). No separate VPS compose set (AD-042 triad preserved). | Cycle G context.md (D-3) |
| AD-093 | Caddy edge: service in prod overlay only, `deploy/Caddyfile` with `{$LEARNY_DOMAIN}` reverse-proxying solely to `web:3000` (ADR-0017 makes the Next proxy the only public surface; api never exposed); volumes `caddy_data`+`caddy_config` persist certs; only 80/443 published — api/web host ports move from base compose to the dev override (same pattern as infra ports). | Cycle G context.md (D-4) |
| AD-094 | Deploy transport: scp compose base+prod+Caddyfile to fixed `/opt/learny`, then ssh `docker compose ... pull && up -d --no-build --wait` with the sha tag; VPS keeps all runtime secrets locally (`secrets/*.env`, `.env` w/ `LEARNY_DOMAIN`) — CI never transfers secret material; `--wait` turns unhealthy deploys into red runs; worker-pdf included (8 GB VPS sized for it). | Cycle G context.md (D-5) |
| AD-095 | Nightly eval JSONL: eval.yml gains a publish step committing `evals/results/*.jsonl` to dedicated branch `eval-results` (job-scoped `contents: write`), artifact upload retained, absent-key/no-results → green skip; main history stays clean while RFC-002's "results committed as JSONL" is met. | Cycle G context.md (D-6) |
| AD-096 | Presentation split: automatable finale ships in-cycle (README deploy section + Mermaid refresh + decisions/roadmap currency, `docs/ops/deploy.md`, `docs/media/README.md` capture guide + asset slots, `docs/retrospectives/2026-07-learny-v2.md`, 0.2.0 version bump both stacks); demo GIF/screenshots stay a guided manual author task; `gh release create v0.2.0 --generate-notes` runs at ship-cycle wrap post-merge. | Cycle G context.md (D-7) |
| AD-097 | Backup scheduling = dedicated compose sidecar running crond (ships with the scp'd overlay, repo-testable, no host cron/celery-beat coupling). | Cycle v3-A context.md (D-1) |
| AD-098 | Backup tooling = repo-owned image (alpine + postgresql16-client + pinned minio `mc`, shell scripts in `deploy/backup/`), published as 4th GHCR image `learny-backup`; no third-party backup images. | Cycle v3-A context.md (D-2) |
| AD-099 | Offsite topology: DB dumps kept local (retention) AND copied offsite; MinIO bucket mirrored offsite-only (no local duplicate on the same disk); offsite is any S3-compatible endpoint via `LEARNY_BACKUP_REMOTE_*`, secret-gated — unset ⇒ local-only mode with explicit notice. | Cycle v3-A context.md (D-3) |
| AD-100 | Monitoring = single pinned Netdata container in the prod overlay, loopback-only publish (`127.0.0.1:19999`) + SSH tunnel; caddy remains the only non-loopback listener (upholds ADR-0017/0023); Prometheus/Grafana and uptime SaaS rejected for one-operator scale. | Cycle v3-A context.md (D-4, D-7) |
| AD-101 | Runtime image installs main deps only (`uv sync --frozen`, no extras) after an import-chain audit for dev-extra packages; no `prod` extra duplicated. | Cycle v3-A context.md (D-5) |
| AD-102 | pdf-worker slimming is a bounded do-if-clean investigation (CPU-only torch wheel pinning via uv); blocker + evidence recorded in ADR-0024 otherwise. | Cycle v3-A context.md (D-6) |
| AD-103 | RFC-003 cycle reorder (user-confirmed 2026-07-17): key-free Cycle C (`v3-ocr`) runs before Cycle B (`v3-eval-maturity`), which is blocked on provider keys (no repo secrets, no local keys, nightly eval green-skips, no `eval-results` history); B resumes when keys are provided. | Cycle v3-C context.md (D-0) |
| AD-104 | Scanned-PDF OCR trigger = automatic single retry: fast path `do_ocr=False`; on no-extractable-text retry once with OCR; still empty → terminal `InvalidDocumentError`. No per-upload flag, no schema/API change; `LEARNY_PDF_OCR_ENABLED` kill-switch reproduces pre-OCR behavior exactly. | Cycle v3-C context.md (D-1) |
| AD-105 | OCR engine = EasyOCR via Docling (reuses the torch already in the pdf-worker image), models baked at build (`with_easyocr=True`), languages via `LEARNY_PDF_OCR_LANGS` default `en,pt`. | Cycle v3-C context.md (D-2) |
| AD-106 | PDF language via pure stopword-ratio detector (en/pt tables, min-sample + confidence gating, None on ambiguity) run in BuildCorpus only when the parser supplied no language; feeds both the FTS `search_config` trigger and localized normalization. No detection library dependency. | Cycle v3-C context.md (D-3) |
| AD-107 | Normalization heuristics become a per-language table (`None` neutral row = prior constants, `en`, `pt`) keyed by primary subtag; module stays pure; new languages are data-additive. | Cycle v3-C context.md (D-4) |
| AD-108 | Scanned-fixture realization: live-gated (importorskip docling) code-generated image-only PDFs for end-to-end OCR proof; CI keeps the retry logic covered via stubbed converter; `GOLDEN_FIXTURES` stays docling-free and unchanged. | Cycle v3-C context.md (D-5) |
| AD-109 | Notebooks hierarchy deferred out of Cycle E entirely (ADR-0026 marks it optional; tags+backlinks suffice at author scale). | Cycle v3-E context.md (D-1) |
| AD-110 | Highlight capture: client sends selection evidence (quote + 32-char context + markdown offsets); the SERVER resolves block hash/ordinal/in-block offsets at save — section read API unchanged, resolution authority in one place. | Cycle v3-E context.md (D-2) |
| AD-111 | `corpus_blocks.content_hash` (normalized-text sha256) computed at build time, nullable, NO backfill — old corpora self-heal on re-ingest; quote tiers cover NULL-hash blocks meanwhile. | Cycle v3-E context.md (D-3) |
| AD-112 | Note links derive from `[[wikilink]]` parsing on save (case-insensitive title match, unresolved keeps text, SET NULL on target deletion); tags are explicit API-set values, lowercase-normalized. | Cycle v3-E context.md (D-4) |
| AD-113 | A highlight IS a note with an anchor (note body optional; notes hold 0..N anchors) — no separate highlights table; resolves ADR-0026's open cardinality question for E. | Cycle v3-E context.md (D-5) |
| AD-114 | Note-anchor statuses reuse the quiz vocabulary exactly (active/stale/orphaned; relocation stays active and rewrites the anchor). | Cycle v3-E context.md (D-6) |

## Blockers

- None. AD-006/007/008 resolved.

## Known Gaps (non-blocking)

- `backend/pyproject.toml` pins `ruff>=0.9,<1`; the resolved `ruff` (0.15.20) formats
  10 Cycle-1 files differently than whatever ~0.9.x originally formatted them
  (`uv run ruff format --check .` fails on those 10, confirmed pre-existing on
  `main` prior to Cycle 2). `ruff check .` (lint) and all test gates are
  unaffected. Deliberately not fixed during Cycle 2 (source-storage) to avoid
  scope creep — user chose to defer. Follow-up: pin ruff to an exact version or
  do one dedicated repo-wide reformat commit.

## Handoff

- **Cycle v3-E `v3-notes-foundation` (RFC-003 Cycle E — flagship capture+organize per ADR-0026) — built on `feat/v3-notes-foundation`, Verifier PASS, publishing in progress. USER INSTRUCTION: this is the session's FINAL cycle — after merge + wrap, STOP (Cycle F and key-parked Cycle B are for future sessions).** 10 commits `586c523..ab4a69f` across 4 phases (A–D worker-per-phase, all Opus): A migration `0010_notes_schema` (notes/note_anchors/tags/note_tags/note_links copying the 0008 snapshot shape with the INVERSE cascade — note_anchors.source_id is a bare UUID, no FK to sources/corpus_*; note_links.target SET NULL; per-user lowercase tag uniqueness) + `corpus_blocks.content_hash` (build-time normalized-sha256 via CorpusSectionRecord.block_hashes, nullable, no backfill per AD-111) + pure AnchorResolver (first-block binding for spanning selections, prefix/suffix disambiguation, NULL-hash tolerance); B entities/ports/Connection-repos + CRUD/capture use cases (owner-collapse, body cap `LEARNY_NOTES_MAX_BODY_CHARS=100000`, wikilink derive w/ self-link ignore, tags lowercase, StaleCaptureTarget 409) + ReconcileNoteAnchors 4-tier cascade wired after quiz reconcile in the worker (own txn; tiers: anchor/alias+block-hash → quote-in-section rebind → quote-across-doc alias-aware relocate → orphaned-kept-forever; stale = live anchor, quote gone; NF-08 SPEC_DEVIATION accepted: no product source-delete call site exists — capability = NoteRepository.orphan_anchors_for_source + reconcile-path orphaning, proven by live-DB source-delete test); C 7-route API (`/api/notes` CRUD+backlinks + `/api/sources/{id}/highlights` capture; rate_limit_notes; error maps; capture wired w/ Bs4MarkupConverter; design call accepted: capture ONLY on the highlights route, POST /api/notes stays plain create) + sanctioned GetBacklinks addition; D `lib/notes.ts` (contract mirrored 1:1 — capture sends quote+32-char context, NO offsets: client-side intermediate only), capture-popover in section-reader (selection→served-markdown match, hides on formatting-only selections, 409 stale path), NotesScreen/NoteDetailScreen (textarea+Streamdown preview toggle, tags chips, backlinks panel, jump-back links, distinct orphan badges w/ quote snapshot), sidebar Notes entry. Decisions AD-109..AD-114; spec NF-01..14. Verifier: **PASS — 14/14 ACs, 7/7 sensor mutants killed (1 fix iteration: orphan jump-back gate direction pinned via distinct orphan fixture identifiers in `ab4a69f`), 0 gaps** (`validation.md`). Gates: backend **777 passed / 402 skipped** offline + ruff clean; DB-gated notes suite 22 passed (live test DB); frontend **210 passed** + tsc clean. ENVIRONMENT NOTES: (1) `backend/.env` now exists locally with REAL OpenAI+Anthropic keys (gitignored, verified untracked — NOT a leak) → **Cycle B's local key prerequisite is now met**; repo secrets still unverified; ~10 unrelated tests fail when run WITH the test DB + .env (real-key adapter identity asserts) — offline suite is the baseline, CI unaffected; (2) uncommitted USER work in `frontend/app/components/library-screen.tsx` + `frontend/tests/sources-screen.test.tsx` (confirm-gated re-ingest control) kept strictly out of all cycle commits/PR — still sitting in the working tree for the user. Next: PR → fresh review → triage → auto-merge (standing approval) → wrap → STOP.
- **Cycle v3-D `v3-notes-research` (RFC-003 Cycle D — research-gated flagship, part 0) — research + ADR-0026 (Proposed) built on `feat/v3-notes-research`, MERGED to `main` (PR #30, merge commit 2af7ac3); ADR-0026 ACCEPTED by the user 2026-07-18 — Cycles E–F UNBLOCKED.** Research fleet: 7 parallel researchers + adversarial verifiers + critic (15 agents, ~1.3M tokens); 13/15 completed — rq05's verifier and the completeness critic died on a session limit and were executed INLINE by the orchestrator (rq05: 2 claims CONFIRMED in-repo incl. corpus_chunks CASCADE lifecycle and document_index citation mapping; pricing claim substance-confirmed; NotebookLM claim corroborated by rq01's own correction). Two refuted claims corrected in place (rq01: NotebookLM lock-in overstated — has notes/export/enterprise API, still no SRS/self-hosting; rq04: Lexical v0.48.0 not breaking, v0.44–47 were). Reports: rq01 competitive (niche CONFIRMED open; RemNote/Heptabase closest; table-stakes list), rq02 anchoring (layered value-based selector + 4-tier exact cascade + orphans-forever, fuzzy deferred), rq03 data model (whole-Markdown notes + derived indexes + note_anchors snapshots, no corpus cascades), rq04 editor (textarea+streamdown now, CodeMirror 6 upgrade path, Lexical rejected), rq05 retrieval (parallel notes index as 2 extra RRF arms + weight + toggle, never in corpus_chunks), rq06 note→quiz (creation-minted stable identity, FSRS never implicitly reset), rq07 export (one-way Obsidian vault projection). synthesis.md carries the inline completeness critique (1 cross-report tension resolved: block-ID persistence via reconcile-on-save match) + RFC assumption check (anchor scheme extension is additive — invalidation branch NOT triggered). ADR-0026 Proposed with 7 decision blocks, independently revisable (1↔2 coupled). This is a docs-only cycle: the PR is the user's review surface and ADR acceptance is the merge-and-gate decision — the agent pr-review pipeline does not apply (precedent: RFC-002/003 planning docs). Next: user reads ADR-0026 → accept/revise → on acceptance, `v3-notes-foundation` (Cycle E) becomes runnable; `v3-eval-maturity` (Cycle B) still parked on API keys.
- **Cycle v3-C `v3-ocr` (RFC-003 Cycle C, run before B per AD-103) — MERGED to `main` (PR #29, merge commit 63ddcf5).** Review (user-approved fresh reviewer agent after the inline-execution question): 6 lanes, 4 findings — 1 real fixed (`52928c7` OCR-retry error-leg test: docling error on the second pass → terminal, never a third conversion), 3 won't-fix with rationale (self-verification process note — compensated by sensor + in-image proof + this review; docling API stub-asserted in CI — verified against pinned source AND executed in-image; pt→portuguese FTS leg transitive — both links directly tested), 0 false; security/performance/regression/architecture lanes all zero-finding, behavior-preservation independently verified against main. All 3 PR comments cleaned; final CI 4/4 green; post-fix gate **739 passed / 350 skipped** + ruff clean. Auto-merged under standing approval. Process deviation noted: triage record rode in the fix commit rather than its own docs(specs) commit. Build record below. 10 commits `c5981f5..d388bc7`, executed INLINE by the orchestrator (user instruction after the Phase A worker died on the account session limit; author ≠ verifier separation therefore unavailable — verification ran as the tlc standalone fallback with a 6/6-killed discrimination sensor + an executed in-image proof; flagged in validation.md for the reviewer). Slice: selective OCR (fast path `do_ocr=False`; exactly-one retry with EasyOCR `lang` from `LEARNY_PDF_OCR_LANGS` on successful-but-textless conversions only; conversion errors never retry; `LEARNY_PDF_OCR_ENABLED=false` reproduces pre-OCR behavior byte-identically), `pdf` extra gains `easyocr>=1.7,<2` (docling 2.11x does NOT bundle it — discovered by the real image build; additive 8-package lock change, CI dev-path untouched) + `libgl1/libglib2.0-0/libxcb1` in the pdf-worker stage + `with_easyocr=True` bake; stopword language detection (`app/application/language.py`, density+ratio gated, None on ambiguity) wired in BuildCorpus only when the parser declared no language → PDFs now get `pt`/`en` into the corpus language column (→ portuguese FTS via existing 0007 trigger); normalization heuristics table (`LanguageHeuristics`, neutral==en==historical constants, pt row: capitulo/parte/seção stem patterns + part/chapter keyword flat-hierarchy fallback needing both ranks). In-image proof under `-m 4g`: scanned bitmap-font PDF ingests via OCR in 47.5s w/ baked models (no egress), kill-switch + blank terminal; image ~7.0GB. Gates: backend **738 passed / 350 skipped** + ruff clean; goldens byte-identical (OCR-15). Decisions AD-103..AD-108; spec OCR-01..18. Next: PR → review (mode pending user decision — see below) → triage → auto-merge (standing approval) → `v3-eval-maturity` once keys exist.
- **Cycle v3-A `v3-ops-maturity` (RFC-003 Cycle A — backups + monitoring + image hygiene) — MERGED to `main` (PR #28, merge commit 6c843b3).** Review triage: 12 findings — 10 real (7 fixed: lock/offsite-guard test discrimination on executed lines via `_executed_lines` helper, `/etc/backup.env` umask 077, hardcoded `MC_SHA256` build ARG replacing same-origin TOFU checksum, `pg_restore --single-transaction` all-or-nothing restores, CI offsite proof via stand-in bucket on the same MinIO (+ `mc mb --ignore-existing` in the offsite branch), netdata `DISABLE_TELEMETRY=1` + explicit trust-boundary docs in monitoring.md/ADR-0024; 3 won't-fix: mc argv creds (isolated namespace, MC_HOST URL-encoding footgun), internal source-endpoint var undocumented by design, structural-only heartbeat/failure edges pre-recorded), 1 false (ADR three-digit H1 IS house style), 1 pre-recorded boundary — `review-triage.md`. Fix commits `21d2b1d..b67ac34` + triage record `9443c93`. Post-fix gates: backend **711 passed / 350 skipped** + ruff clean; final CI 4/4 green with BOTH backup roundtrips (local-only + offsite) executing. All 11 PR comments cleaned, no un-deletable reviews. Auto-merged under the user's standing approval (2026-07-17). Build record below. 10 implementation commits `db5e370..157c431` across 5 phases (A–D worker-per-phase all Opus, E inline): A backup engine — repo-owned `deploy/backup/` image (alpine:3.22 pinned, postgresql16-client, checksum-verified mc RELEASE.2025-08-13T08-35-41Z, crond entrypoint `LEARNY_BACKUP_CRON` default 30 3 * * *) with `backup.sh` (set -euo pipefail, flock, pg_dump -Fc tmp+rename, offsite gated on all four `LEARNY_BACKUP_REMOTE_*` else "offsite not configured" exit 0, mc mirror without --remove, prune KEEP_DAYS=14 newest-exempt post-success-only, heartbeat success-only-last) + `restore.sh <archive|--latest> --yes` (pg_restore --clean --if-exists --no-owner; no-`--yes` = plan + non-zero), prod overlay `backup` service (GHCR learny-backup, env_file db/minio/backup.env required:true, backup_data vol, no ports) + dev-override profile, deploy.yml 4th matrix image, `.env.production.example` backup section, `test_backup_stack.py`; B ci.yml compose-smoke roundtrip (seed→backup-now w/ offsite-notice grep→drop→restore.sh --latest --yes→assert row) + backups.md automation rewrite (deferral text removed) + deploy.md backup.env + test_ops_docs; C `monitoring` netdata v2.10.4 (Hub-verified) per official recipe except deliberate loopback-only 127.0.0.1:19999 publish (vs host networking — preserves caddy-only-non-loopback, test tightened to loopback-aware `test_prod_publishes_non_loopback_ports_only_on_caddy`), monitoring.md runbook (SSH tunnel, routine checks, backup-log reading); D runtime image drops `--extra dev` + non-root uid 10001 (import audit: docling only behind lazy PDF-branch probe; verified by building+booting image) — pdf-worker torch CPU pin BLOCKED upstream (torchvision 0.28.0 `<2.13.0+` excludes +cpu local versions; ~2.6GB CUDA wheels measured; evidence in ADR), `test_compose_topology` runtime asserts; E ADR-0024 (closes backup/monitoring half of TDD OQ #10, records OPS-18 blocker). Decisions AD-097..AD-102; spec OPS-01..19. Verifier: **PASS — 19/19 ACs, 6/6 sensor mutants killed (1 fix iteration: restore-flags test strengthened to executed-command-line assert in `157c431`), 0 gaps** (`validation.md`); lesson L-010 candidate recorded (whole-file substring asserts don't discriminate command-line regressions). Gates: backend **704 passed / 350 skipped** (frozen-lock baseline; count re-normalized from drifted local env) + ruff clean; both compose merges `config -q` valid; frontend untouched. Worker idle-without-summary happened 3× (C, D, Verifier) — nudge via SendMessage worked each time. Next: learny-finalize PR → pr-review → triage → auto-merge (user standing approval 2026-07-17) → next cycle `v3-eval-maturity`.
- **RFC-003 (v3 roadmap) accepted 2026-07-17** — flagship: notes & second-brain (research-gated by Cycle D's notes-domain ADR; Cycles E–F provisional until it lands); supporting tracks: ops maturity, eval maturity, scanned-PDF OCR + localized normalization. Excluded: BYOK, hosted instance, vector DB/reranker, MCQ, FSRS optimizer, eval dashboard. Scope chosen by the user in-session (flagship picked over hosted-instance / learning-loop-deepening / consolidation-only; all four supporting-track candidates offered, BYOK declined). ROADMAP.md now carries the v3 table (`v3-ops-maturity` → `v3-notes-loop`); CLAUDE.md status refreshed. **Next ship-cycle row: `v3-ops-maturity` (RFC-003 Cycle A — backups + monitoring closing TDD OQ #10, ADR-0023 image-hygiene follow-ups).** Still pending outside cycles (user-gated, from Cycle G): tag `v0.2.0` + `gh release create v0.2.0 --generate-notes`; flip 3 GHCR packages public after first image push; demo media capture; Dependabot PRs #18/#19 (actions/checkout & setup-node v4→v7) open and unreviewed.
- **Cycle G `v2-deploy` (RFC-002 Cycle G — deploy + presentation) — MERGED to `main` (PR #27). This completes RFC-002 (cycles A–G all shipped).** Review triage: 5 findings, all real + all fixed (`review-triage.md`) — a High-severity `workflow_run` fork-escalation gap (deploy guard now requires `head_repository.full_name == github.repository` on both jobs), two Critical runbook-accuracy bugs from the Haiku-authored `deploy.md` (phantom `backend/secrets/*.env.production.example` path → real `backend/.env.production.example`; unprefixed/omitted env vars — `SECRET_KEY`/`ANTHROPIC_API_KEY`/`STORAGE_*` rewritten to `LEARNY_`-prefixed against the template, per-service env_file corrected), and two test-strength gaps (deploy dispatch-main-ref guard, Caddyfile `{$LEARNY_DOMAIN}` TLS address); fix commits `f1ed067..7d2ea88` (incl. a lockfile 0.2.0 sync D3 missed); all 7 PR comments cleaned, no un-deletable reviews. Final CI: 4/4 green. Post-fix gate: deploy suites **50 passed** + ruff clean. Model-cost note (lesson): the runbook was the one Haiku-authored phase and produced both Critical findings — doc chores with real-consequence factual content (env var names) are not slip-proof; review caught them. Remaining v0.2.0 release checklist (post-merge, user-gated): tag `v0.2.0` + `gh release create v0.2.0 --generate-notes`, flip the 3 GHCR packages public after the first image push, record the demo GIF/screenshots into `docs/media/` slots. Below is the pre-merge build record. 13 commits `2d21880..c108680` across 5 phases (A–C Opus, D Haiku, E Opus): A compose edge — app host ports moved base→dev override so prod can close them, prod overlay consumes `ghcr.io/augusto-dmh/learny-{backend,pdf-worker,web}:${LEARNY_IMAGE_TAG:-latest}`, new `caddy:2.10-alpine` service (sole public surface, only 80/443/443-udp, `caddy_data`/`caddy_config` volumes, `deploy/Caddyfile` reverse-proxying only `web:3000` per ADR-0017, `LEARNY_DOMAIN:?` fail-fast) + `test_deploy_topology.py`; B `deploy.yml` — `workflow_run`(CI success @ main)+dispatch-gated, `concurrency: deploy` non-cancelling, 3-image build matrix (runtime/pdf-worker/prod targets) via `docker/build-push-action@v6` tagged sha+latest with job-scoped `GITHUB_TOKEN`, then `needs`-ordered secret-gated (VPS_HOST/USER/SSH_KEY absent → green skip) raw scp+ssh deploy of the compose triad+Caddyfile to `/opt/learny` running `pull && up -d --no-build --wait` with the sha tag, no secret material transferred + `test_deploy_workflow.py`; C `eval.yml` gains job-scoped `contents: write` + a publish step committing nightly `evals/results/*.jsonl` to the dedicated `eval-results` branch (`results/<date>-<run_id>/`, rebase-retry-once, never force-push, artifact upload retained) + `test_eval_workflow.py`; D `docs/ops/deploy.md` VPS runbook + rollback.md image-tag path, `docs/media/README.md` capture guide + asset slots, version bump to 0.2.0 both stacks + `test_versions.py`; E ADR-0023 (resolves the TLS/proxy half of TDD OQ #10), README brought current to v2 (status/architecture+Caddy Mermaid edge/tech-stack/roadmap, 23 ADRs), `docs/retrospectives/2026-07-learny-v2.md` (cycles A–G). Decisions AD-090..AD-096; spec DEP-01..20. Verifier: **PASS — 20/20 ACs (15 test-anchored, 5 doc-verified), 4/4 sensor mutants killed, 0 gaps** (`validation.md`). Gates: in-scope suite **49 passed** + ruff clean; full backend 654 passed/349 skipped locally (DB/live/docling skips pre-existing); frontend 174 passed + tsc. Deploy machinery is provider-neutral (no VPS vendor/domain locked). Merge-gate flags: (1) first cycle to publish images + require VPS/GHCR secrets — CI/local stay build-based and offline; (2) demo GIF/screenshots remain a manual author task (guide shipped, slots empty); (3) `runtime` image still installs the dev extra + pdf-worker image is multi-GB — recorded as ADR-0023 follow-ups. Next: pr-review → triage → merge gate → tag v0.2.0 + `gh release create v0.2.0 --generate-notes` + flip GHCR packages public.
- **Cycle F `v2-ingestion-breadth` (RFC-002 Cycle F — PDF via Docling + corpus normalization) — built on `feat/v2-ingestion-breadth`, Verifier PASS, publishing in progress.** 12 implementation commits `633474c..989ab78` across 6 phases (one worker each; A–E Opus, F Haiku): A port rename (`DocumentParserPort`/`InvalidDocumentError`, DTO extensions `ParsedSection.anchor_aliases`/`ParsedBlock.page_span`) + pure `normalize_book` pass (title cascade, flat-TOC hierarchy inference, depth clamp, trivial merge w/ aliasing, Gutenberg strip — F7 fix); B migration `0009_anchor_aliases` (TEXT[] on corpus_sections) + alias-aware `get_section`/`section_texts`/`expand_anchors` + BuildCorpus wiring w/ `corpus_normalized` event + `pack_chunks(page_spans=)` + alias-aware quiz reconcile + teaching anchor expansion; C `DoclingPdfParser` (pure mapping vs docling-core + `_convert` on docling 2.113.0, AD-086 anchors, page spans) + `build_parser` dispatch factory (worker dispatch by filename, SPEC_DEVIATION marked, equivalence via validation's ext↔MIME agreement); D upload validation format table (`.pdf`/`application/pdf`, `LEARNY_PDF_MAX_BYTES=100MiB`) + enqueue routing (`PDF_INGEST_QUEUE="ingest-pdf"` in the Celery enqueuer adapter); E multi-stage Dockerfile `pdf-worker` target (baked layout+tableformer models, no OCR) + compose `worker-pdf` (queues ingest-pdf, conc 1, mem 4g, max-tasks-per-child 1) + `worker --queues celery` pin + ci.yml off `--all-extras` + compose-smoke scoped to non-pdf services + topology tests; F ADR-0022. Decisions AD-082..AD-089; spec ING-01..24. Verifier: **PASS — 24/24 ACs, 7/7 sensor mutants killed (P0-full), 0 gaps** (`validation.md`). Gates: backend **941 passed, 11 skipped** + ruff clean (CI stays docling-free: 916 passed/12 skipped proven without docling); frontend untouched. Accepted deviations recorded in phase summaries + validation.md (SectionChunk.page_span type widening, no_toc golden merge, EpubCorpusIngestionStep name kept, encrypted-PDF shares corrupt branch). Next: pr-review → triage → merge gate.
- **Cycle E `v2-active-recall` (RFC-002 Cycle E — Flagship: active recall quizzes + FSRS) — built on `feat/v2-active-recall`, Verifier PASS, publishing in progress.** 18 implementation commits `d9f6880..0b651b6` across 6 phases (one Opus worker each): A foundation (deps `fsrs==6.3.1`+`genanki==0.13.1`, 8 `LEARNY_QUIZ_*`/`LEARNY_FSRS_*` settings, migration `0008_quiz_schema` — `quiz_items`/`quiz_item_scheduling`/`review_log`/`quiz_generation_jobs`, no corpus FK, `(source_id,content_key)` unique, rating CHECK 1..4 — domain contracts + `quiz_qc.py`); B repos (upsert preserves scheduling/log; due query owner-joined) + `DeterministicQuizAdapter` + `AnthropicQuizAdapter` (Message Batches, structured outputs verified legal in batch params on `anthropic==0.116.0` — no fallback needed, `source_chunk_id` enum per section) + factory; C `PlanDeckGeneration`/`RunDeckGeneration` (QC→dedup→upsert pipeline) + `generate_quiz_deck`/`poll_quiz_deck` Celery tasks (self-reschedule, deadline timeout) + `ReconcileQuizItems` wired in `run_ingestion` after corpus replace (keep/stale/relocate/orphan; scheduling+log never touched; FSRS adapter created early here for initial()); D FSRS unit suite + `GetDueQueue`/`SubmitReview` + `web/quiz.py` 5 endpoints (rate_limit_quiz, error mappings, commit-then-enqueue) + genanki export (builtin BASIC/CLOZE models, `guid_for(source_id, content_key)`); E `lib/quiz.ts` + `/review` ReviewScreen (queue→reveal→grade→advance→summary) + library deck controls (`use-quiz-deck-polling` 3s hook) + sidebar Review entry; F deterministic groundedness eval (poisoned-candidate discrimination case) + answerability judge (`live and eval`, versioned prompt, JSONL; eval.yml unchanged — marker collects it) + ADR-0021. Decisions AD-072..AD-081; spec QUIZ-01..25. Verifier: **PASS — 25/25 ACs matched, 6/6 sensor mutants killed (P0-full), 0 spec-precision gaps** (`validation.md`). Gates: backend **833 passed, 11 skipped** + ruff clean; frontend **174 passed** + tsc + build. Full vertical slice (schema+worker+API+UI+eval). Notable worker deviations (all flagged, accepted): quiz metadata tables added in B (A2 shipped migration only); FSRS adapter authored in C, unit-suited in D (commit is `test(quiz)`); deck polling extracted to a hook mirroring `use-ingestion-polling` (component fake-timer test OOMed); QuizDeckControls renders nothing until overview loads (keeps strict routedFetch tests green); local free_recall/cloze near-duplicates legitimately dedup-discarded (bag-of-tokens embeddings) so eval asserts invariants not fixed counts. RFC-002 follow-up ADR list deliberately not back-annotated (matches 0019/0020 convention). Next: learny-finalize PR → pr-review → triage → merge gate.
- **Cycle D `v2-frontend` (RFC-002 Cycle D — Frontend v2: product UI + streaming) — built on `feat/v2-frontend`, Verifier PASS, publishing in progress.** 20 implementation commits `7504e45..0c68f33` across 5 phases (one worker each): A styling foundation (Tailwind v4 `^4.3.3`, shadcn radix-nova preset CSS-variables, AI Elements vendored+pruned to import closure, `ai@^7.0.30`+`@ai-sdk/react@^4.0.33`, geist fonts, next-themes, `(app)`/`(auth)` route groups with URLs unchanged); B seams (`GET /api/sources/{id}/section?anchor=` 200/404/401/422 via `CorpusRepository.get_section`+`ReadSection`+`SectionContent` entity, `lib/ingestion.ts`+`lib/sections.ts` clients, SSE proxy-relay regression test); C shell (AppSidebar w/ status badges + collapsible book tree, AuthHeader w/ theme toggle, LibraryScreen w/ upload+failed-state+restart, `use-ingestion-polling` 3s/terminal-stop/unmount-cleanup); D streaming (`lib/streaming.ts` transports + `streamingFetch` status-mapping + `turnsToUIMessages` + `errorMessageFor`, citations popover on shadcn Popover [SPEC_DEVIATION: AI Elements InlineCitation/Sources model web sources], AskScreen + TeachScreen on `useChat` w/ real v1 SSE fixture tests; old panels deleted); E reader (`/sources/[id]/read?anchor=` SectionReader on `MessageResponse`/Streamdown, scroll+highlight, not-found/empty states, raw-HTML-inert XSS test, citation→reader round-trip closure). Decisions AD-065..AD-071; spec FE-01..22. Verifier: **22/22 ACs matched, 6/6 sensor mutants killed (both stacks), 0 gaps** (`validation.md`; two non-blocking edge-case notes: no dedicated 403-CSRF component test, no dedicated mid-stream-disconnect-sans-error-part test). Gates: frontend **130 passed / 22 files** + tsc + build; backend **656 passed, 10 skipped** + ruff clean. Full vertical slice restored after 5 backend-only cycles (AD-065 closes the AD-064 flag chain). React exact-pin bumped 19.1.1→19.1.2 (@ai-sdk/react peer). Next: learny-finalize PR → pr-review → triage → merge gate.
- **Cycle C `v2-generation` (RFC-002 Cycle C — Claude generation) — MERGED to `main` (PR #23).** 15 implementation commits `1500fd7..311622b` across 4 phases (one Opus worker each): A settings + `anthropic>=0.116,<1` + `AnthropicAnswerAdapter` (per-chunk citations-enabled documents, `document_index`→chunk_id mapping, whole-reply `NOT_FOUND_IN_SOURCE` sentinel fixing F5) + answer factory/DI + ADR-0020; B `AnthropicTeachingAdapter` (alternating history, `ttl:"1h"` cache breakpoints on frozen system prompt + latest history turn, volatile evidence after prefix) + teaching factory/DI; C streaming (`AnswerTextDelta`/`AnswerCompleted` + `generate_stream` on both ports, application streams with sentinel hold-back + persist-on-completion, SSE `/questions/stream` + `/turns/stream` emitting UI Message Stream v1 via `ui_message_stream.py` presenter, fastapi floor →`>=0.135`); D eval (replay snapshots + `--record-generation`, exact citation invariants every PR, `app/eval/judge.py` faithfulness/relevancy judge with structured outputs on Haiku + versioned prompts + JSONL results, live smoke incl. F5 sentinel proof, nightly `eval.yml` cron+dispatch secret-gated with `LEARNY_EVAL_MAX_CASES`). Decisions AD-058..AD-064; spec GEN-01..23. Verifier: **23/23 ACs matched, 6/6 sensor mutants killed (P0-full tier), 0 gaps** (`validation.md`). Gate: backend **645 passed, 10 skipped** (8 skips by design: 5 live/eval key-gated, 3 snapshot-absent) + ruff clean. Deterministic adapters stay the CI/offline default (`LEARNY_GENERATION_PROVIDER=local`). Worker deviations accepted in review: SENTINEL canonical home moved to `domain/entities.py` (application-layer purity; prompts.py re-exports), shared `_AnthropicAdapter` base, shared 502-body constant, `_preflight()` extraction in teaching. Session-limit incident: first Phase A worker died before any commit; relaunched clean. Merge-gate flags: (1) 5th consecutive backend-only slice (AD-064); (2) real replay snapshots + judge-threshold gating deferred until a key records baselines (AD-062/AD-063); (3) Sonnet 4.6 pinned per RFC — Sonnet 5/Opus 4.8 are settings-swap alternatives in ADR-0020. Next: learny-finalize PR → pr-review → triage → merge gate.
- **Cycle B `v2-embeddings` (RFC-002 Cycle B — real retrieval) — MERGED to `main` (PR #20).** Review triage: 4 findings, all real + all fixed (perf: reembed `LIMIT` pushed into SQL; arch: collapsed duplicate `embedding_dim`/`embedding_dimensions`; two test-strength: HNSW index-definition assertion + recall@5 total-failure-guard doc) — see `review-triage.md`; 3 fix commits `48ee5b9..a0c2d26`; all PR comments cleaned. Final CI: 4/4 jobs green. Post-fix gate: backend **554 passed, 2 skipped** + ruff clean. 14 implementation commits `c6adce8..2523c29` across 4 phases (one worker each): A OpenAI adapter + provider factory + `EmbeddingPort.model` + ADR-0019 (no DB), B schema 0007 + language-aware FTS trigger fixing F8 (DB), C `reembed_document` task + per-chunk model versioning (DB), D tier-2 recall@k/MRR eval (DB) — plus one orchestrator hardening commit (multi-batch reembed resumability test). Decisions AD-051..057; spec EMB-01..22. Verifier: **22/22 ACs COVERED, 5/5 sensor mutants killed, 0 gaps** (`validation.md`); two spec-precision notes — EMB-17 resumability closed post-verify by the hardening test, EMB-22 recall@5 kept by design (shared 3-chunk golden fixture; combined recall@1+MRR+recall@5 gate bites both ranking and total-failure). Gate: backend **554 passed, 2 skipped** (both `@pytest.mark.live` OpenAI, offline) + ruff clean. F8 empirically proven (`correr`→`correndo` matches under `portuguese`, `[]` under `english`). Deterministic adapter stays the CI default (offline). Merge-gate flags: (1) 4th backend-only slice (AD-051); (2) first paid provider SDK + prod key requirement — CI/local stay offline on the deterministic default (AD-052); (3) committed eval snapshot is deterministic-model (`local-deterministic@1536`), real `text-embedding-3-large@1536` snapshot deferred to a keyed follow-up (AD-056). Next: learny-finalize PR → pr-review → triage → merge gate.
- **Cycle A `v2-foundation` (RFC-002 Cycle A — first v2 cycle) — MERGED to `main` (PR #17), tagged `v0.1.0`.** Verifier PASS (19/19 ACs, 6/6 mutants, 1 fix iteration). Review triage: 12 findings, 11 real, all fixed (incl. two phantom CI action versions and a MinIO-in-CI gap the live run confirmed) — see `review-triage.md`; PR comments cleaned. Final CI: 4/4 jobs green (backend 507 tests vs Postgres+Redis+MinIO services). Next roadmap row: Cycle B (real embeddings). 11 commits: docs landing (README, QA runbook+report, research fleet evidence, RFC-002), F1 compose worker-storage fix + regression test, F2 storage-fault classification (BotoCoreError→StorageUnavailable across the s3 adapter, 5 new tests), F3 proxy header hygiene (strip Expect/hop-by-hop on requests, content-encoding/length on responses, 3 new tests), F4 first-run determinism (env.py honors caller URL; discriminating regression test; full suite passes twice on fresh DBs: 504 passed), CI workflow (4 jobs), Apache-2.0 + SECURITY/CONTRIBUTING/dependabot, CLAUDE.md/ROADMAP refresh. Decisions AD-045..AD-050; spec FND-01..19. Gates: backend 504 passed + ruff clean; frontend 95 vitest + tsc + build; compose boots healthy. Next: Verifier → learny-finalize PR → pr-review → triage → merge gate → tag v0.1.0 on main.

- **Cycle 9 `production-readiness` (TDD Phase 10 — the final MVP roadmap row) — MERGED to `main` (PR #16).** Review triage: 5 findings, all real + all fixed (1 security fail-fast on missing prod secret files, 4 test-coverage/config gaps) — see `review-triage.md`; 4 fix commits `7853678..ec750b7`; all PR comments cleaned. Post-fix gate: backend **497 passed** + ruff clean. 8 commits `fd4f8ec..28668e4` across 3 phases: A (observability) — `feat(observability)` trace context + `LEARNY_LOG_FORMAT` (`app/core/tracing.py`), structured JSON logging with preserved secret redaction (`app/core/logging.py` `JsonFormatter` + idempotent `configure_logging`), request-ID/trace-field middleware + access log (`app/infrastructure/web/middleware.py`, wired in `main.py`, `user_id` bound in `resolve_current`), worker trace binding + durations (`app/worker/tasks.py`, `celery_app` `worker_hijack_root_logger=False`); B (deploy) — hardened prod compose **overlay** with base/override/prod split (`docker-compose.yml` secret+infra-port-free, `docker-compose.override.yml` local, `docker-compose.prod.yml` prod), env.production examples, `secrets/` gitignored; frontend production image (`frontend/Dockerfile` multi-stage + `next.config.ts` `output: standalone`); C (docs) — `docs/ops/backups.md` + `docs/ops/rollback.md`. Decisions AD-040..AD-044; spec `PROD-01..20`. Verifier: **20/20 ACs covered, 7/7 sensor mutants killed** (one surviving mutant M4 closed post-verify by `test(observability)` commit `28668e4` asserting the worker trace-correlation seam), full suite green + stable (`validation.md`). Gates: backend **494 passed** + ruff clean; frontend **build ok + tsc clean + 92 vitest**. Executed inline (3 phases). **Provider-neutral by design — no monitoring/metrics/TLS/reverse-proxy/VPS vendor locked.** NOTE for the merge gate: (1) AD-044 backend+config+docs slice, no frontend feature/schema (deliberate departure from AD-010, precedent AD-023/AD-039); (2) AD-041 defers a metrics scrape endpoint/exporter — correlated structured logs are the hook this cycle ships; (3) **TDD open question #10 (backup/TLS/reverse-proxy/monitoring stack + VPS provider) is now the single blocking project-wide follow-up** — also still open: the cloud LLM + embedding provider ADR (AD-019/AD-024). One in-cycle fix worth noting: A2's original `configure_logging` called `get_settings()`, which primed the lru-cache and made Alembic `env.py` read a stale DB URL under test — fixed by reading `LEARNY_LOG_FORMAT` from `os.environ` directly (candidate lesson L-007).
- **Cycle 8 `golden-fixtures` (TDD Phase 9) — built on `feat/golden-fixtures`, Verifier PASS, publishing in progress.** 6 commits `eb12808..53f4848`: A1 golden fixture corpus + hand-authored expected values (`golden_corpus.py`, `golden_expected.py`, `test_golden_fixtures.py`), A2 ingestion golden (`eval_runner.py` pure runner + `test_golden_ingestion.py`), B1 retrieval recall golden (`test_golden_retrieval.py`), C1 citation grounding golden (`test_golden_citations.py`) — plus two prerequisite test-isolation fixes: `fix(migrations)` dropped alembic env.py `fileConfig` (was clobbering app-owned root logging incl. the redaction filter — a real in-process-migration production risk) and `test(migrations)` restore-to-head fixture (test_migrations downgrades to base and left the shared DB broken once a DB test sorted before it). Decisions AD-036..AD-039; spec `EVAL-01..10`. Verifier: **10/10 EVAL covered, 3/4 sensor mutants killed + 1 characterized non-defect** (grounding-filter mutant is structurally undetectable by the extractive adapter; covered by pre-existing QA unit tests), 1 non-blocking spec-precision note on EVAL-07 (`validation.md`). Gate: backend **453 passed** (+4 golden files) + ruff clean; no frontend/schema (AD-039, backend/test-only slice — flag at merge gate, precedent AD-023). Realized as a versioned deterministic **test harness**, not persisted `evaluation_*` tables (AD-036/ADR-016). Executed inline (3 phases). NOTE for the merge gate: AD-039 backend-only departure from AD-010; EVAL-08 grounded-not-found uses the empty-evidence path (deterministic extractive adapter can't itself reject relevant-looking evidence — a property of the deferred cloud-LLM adapter, AD-024).
- **Cycle 7 `teaching-sessions` (TDD Phase 8) — built on `feat/teaching-sessions`, Verifier PASS, publishing in progress.** 15 implementation commits `58fac91..2487efa` across phases A–E (schema 0006, domain contracts + settings, anchor-scoped hybrid retrieval, session/turn repositories with citation snapshots, shared grounding helper + qa delegation, start/read/list + turn services, deterministic teaching adapter, error mappings + rate limit, session + turn endpoints, teaching client + teach screen + sources link). Decisions AD-030..AD-035; spec `TEACH-01..24`. Verifier: **24/24 ACs matched, 6/6 sensor mutants killed, 0 gaps** (`validation.md`; first Verifier sub-agent died on an account session limit, relaunched per user instruction). Gates: backend **426 passed** (+75) + ruff clean; frontend **88 passed** (+28) + tsc clean. Executed via one sub-agent worker per phase (A–E). NOTE for the merge gate: teaching prose remains deterministic-extractive until the cloud LLM provider ADR (blocking follow-up since AD-024; also covers the embedding provider from AD-019).
- Cycles 1–5 (`scaffold-and-identity`, `source-storage`, `worker-foundation`, `epub-corpus-pipeline`, `retrieval-indexes`) all **merged to `main`** (PRs #4, #7/#8, #9, #10, #12). Each shipped with Verifier PASS; details in their `.specs/features/*/validation.md`. Corpus schema/replace semantics: AD-017/AD-018; retrieval layer: AD-019..023.
- **Cycle 6 `cited-qa` (TDD Phase 7) — built on `feat/cited-qa`, Verifier PASS, publishing in progress.** 10 implementation commits `ddf67a2..97285e1` (DTOs, `AnswerGenerationPort` with `model: str`, errors+settings, `DeterministicAnswerAdapter` "local-extractive", `AskQuestion` service with grounding/dedupe/short-circuit guards, questions rate limit + 409/502 mappings, `POST /api/sources/{id}/questions` router + wiring, browser questions client, AskPanel + `/sources/[id]/ask` + ready-row link). Decisions AD-024..AD-029; spec `QA-01..22`. Verifier: **22/22 ACs matched, 6/6 sensor mutants killed, 0 gaps** (`validation.md`). Gates: backend **351 passed** + ruff clean; frontend **60 passed** + tsc clean. One accepted additive SPEC_DEVIATION (port declares `model: str`; context.md Deviations). Executed via one sub-agent worker per phase (A–D) + orchestrator contract-fix commit `7ba63e8`. NOTE for the merge gate: per AD-024 the answer prose is deterministic-extractive until the **cloud LLM provider ADR** (OpenAI vs Anthropic, keys/billing) is accepted — that ADR is now the blocking follow-up for real LLM answers (it can also cover the embedding provider deferred in AD-019).
- **Cycle 5 `retrieval-indexes` (TDD Phase 6) — merged (PR #12).** 10 implementation commits `1bb8e24..0fc2d03` + 3 review-fix commits (batched embedding writes, logged the pgvector-registration guard, dropped a duplicate test step) + planning/triage (schema+migration 0005, retrieval/embedding settings + guarded register_vector, ports+evidence entities, deterministic embedding adapter, embedding-index repo, EmbedCorpus service, embed step + task wiring, hybrid RRF retrieval repo, RetrieveEvidence service, retrieve endpoint). Backend **314 passed**; ruff clean. Verifier: **22/22 ACs matched spec outcome, all edge cases covered, 6/6 sensor mutants killed, 0 gaps** (`validation.md`; run inline via tlc standalone fallback after the Verifier sub-agent hit an account session-limit). Executed via one sub-agent worker per phase (A–E). Added the hybrid retrieval layer: nullable `embedding vector(1536)` + generated `search_vector tsvector` on `corpus_chunks` (migration 0005), `EmbeddingPort` + deterministic local adapter (cloud provider deferred, AD-019), embed+index step in `run_ingestion` (AD-021), hybrid RRF `RetrievalPort` query (AD-022), owner-scoped `POST /api/sources/{id}/retrieve`, no frontend (AD-023). Decisions AD-019..AD-023; spec `RET-01..22`. New backend dep: `pgvector` (0.4.2) + numpy. New settings: `LEARNY_EMBEDDING_DIM/EMBEDDING_MODEL/EMBEDDING_BATCH_SIZE/RETRIEVAL_SEMANTIC_LIMIT/RETRIEVAL_LEXICAL_LIMIT/RETRIEVAL_RRF_K/RETRIEVAL_TOP_K/RETRIEVAL_MAX_TOP_K/HNSW_EF_SEARCH`. Next action: `learny-finalize` for the PR — planning artifacts (`.specs/features/retrieval-indexes/*`, this STATE.md, ROADMAP.md row for Phase 6) still need to land in a commit. NOTE for the merge gate: AD-023 (endpoint but no frontend) is a deliberate partial departure from AD-010's full-slice cadence — surface it to the user.
- Test DB note: retrieval integration tests need the `vector` extension present in `learny_test`; the `pgvector/pgvector:pg16` image already ships it, migration `CREATE EXTENSION IF NOT EXISTS vector` creates it.
- Test DB: integration tests read `LEARNY_TEST_DATABASE_URL` (see `backend/tests/conftest.py`; tests using it skip if unset) — set to `postgresql+psycopg://learny:learny@localhost:5432/learny_test` (a dedicated `learny_test` database, created during Cycle 2 Phase 3; distinct from the app's `learny` DB). MinIO local: `http://localhost:9000` (learny/learny-dev-secret). `db`/`minio` containers started via `docker.exe compose up -d db minio` — the bare `docker` CLI is unavailable in this WSL distro; use `docker.exe compose ...`. `uv` is not on the default shell PATH — it's at `/home/augusto/myenv/bin/uv`.

## Deviations (Cycle 1)

- SPEC_DEVIATION (additive, accepted): Phase B added a 7th domain port `TokenGenerator` so opaque/CSRF token minting stays out of the application layer and is deterministic in tests. Existing ports unchanged. Reflected in design intent; update design.md §3 ports list at finalize if desired.

## Preferences

- User prefers decisions surfaced one at a time with options + a recommendation.
