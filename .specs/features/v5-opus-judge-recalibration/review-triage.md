# Review triage — PR #59 (v5-opus-judge-recalibration)

Review artifacts: 10 inline + 2 PR-level comments (requirements summary 5145605492, consolidated summary 5145860910), six lanes, no blocking defect. Comments are deleted in the cleanup stage; this file is the surviving record.

| # | Source comment(s) | Location | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| F1 | 3692165646 (performance) | `backend/app/eval/judge.py:333` | Judge calls fully serialized while the tier grows 2→18 calls | Real (observation) | **Won't fix** | Nightly runs at a 03:00 cron; 1–4 min of sequential network wait is immaterial. Concurrency would break the ordered fake-client test seam — real complexity for no product value. Cost is bounded by `max_cases` + `_JUDGE_MAX_TOKENS`. |
| F2 | 3692166433 (performance) | `backend/app/eval/judge.py` `_get_client` | No client `timeout`/`max_retries` → SDK defaults (600s × 3 attempts) allow ~9h worst-case nightly vs GitHub's 6h cap; artifact upload never reached | Real | **Fix** | Verified: `_get_client` builds `anthropic.Anthropic(api_key=...)` bare; `eval.yml` has no `timeout-minutes`. At 18 calls the unbounded tail is a genuine CI-budget risk. Fix: explicit `timeout` + `max_retries` on the client — one line, no behavior change on healthy paths. |
| F3 | 3692168346 (tests), 3692174496 (architecture), requirements item 1 | `backend/app/eval/ab.py` `_passes_gate` / `judge_agreement` | `float(line["faithfulness"])` / `int(line["relevancy"])` raise `TypeError` on the null-scored decline lines `run_eval` now writes; reproduced by the lane | Real | **Fix** | Three lanes converged independently; latent today (research-only path) but RFC-005 Cycle C's A/B over this exact tier is the natural trigger — it would break mid-recalibration. ADR-028-consistent fix: pair answered lines only (a decline has no quality scores to agree on); dedicated test with decline lines present. |
| F4 | 3692174567 (architecture), 3692183711 (regression) | `backend/app/eval/ab.py` module docstring | Module-level contract still states the pre-ADR-028 rule ("keeps it in the mean") that this PR reversed; contradicts the updated `TierAggregate` docstring below | Real | **Fix** | The module contract is the first thing a future reader loads; a reversed statement of a live gate convention is exactly the drift ADR-028 exists to kill. |
| F5 | 3692183799 (regression) | `.specs/project/STATE.md` Handoff | The new cycle entry replaced the whole Handoff log (−40 lines): 34 prior cycle records (PRs #12–#58) + two durable test-DB notes deleted, silently, inside the planning commit | Real | **Fix** | My error: the tlc pause protocol's "replace the section body" collided with this repo's additive-log practice (every prior handoff commit was additive). Restore the prior log from git and prepend the new entry. |
| F6 | 3692174406 (architecture), 3692183902 (regression) | `backend/tests/eval/harness.py` `snapshot_eval_inputs` | `citation_valid` computed as containment only — weaker than the canonical rule (`silver._citation_valid`, invariants suite): answered ⇒ cites something, decline ⇒ cites nothing | Real | **Fix** | The same JSONL field feeds the same gate assert with a third, weakest meaning. No live impact (all 12 snapshots satisfy the stronger rule — pinned offline), but aligning the mapping to the canonical predicate is a two-line change that removes a colliding concept. |
| F7 | 3692168918 (tests) | `backend/tests/test_eval_judge.py` | Snapshot-tier → `run_eval` composition exercised only when a key is present; offline coverage stops at `snapshot_eval_inputs` in isolation | Real | **Fix** | Cheap, closes a real seam: one offline test running `run_eval` over `snapshot_eval_inputs(load_snapshots())` with the fake client (9 answered → 18 queued payloads, 3 declines → nulls, gate-on passes with the pinned constants). Also indirectly guards F6's semantics. |
| F8 | requirements summary (uncovered DoD) | research doc | Pre-flight cost estimate exists only post-hoc in the research doc — ordering attested, not evidenced | Real (observation) | **Won't fix** | Not retroactively fixable; the estimate genuinely preceded the runs (stated in-session before the first paid call) and the doc says so. The runbook protocol already mandates the ordering for future runs. |
| F9 | requirements summary (non-blocking notes) | thresholds | Rounding 3.0556→3.1 shaves the margin to 0.456; RELEVANCY_MIN move changes per-case `_passes_gate` bar 3→4 for historical comparisons | Real (observations) | **Won't fix** | The documented derivation rule ("rounded to one decimal") was followed and the rounding is conservative (stricter gate). Historical A/B comparisons are era-bound to their own constants; the study archive retains its verdicts. |
| F10 | security lane | — | Zero security findings | — | No action | Clean scan; fail-closed credential wiring highlighted positively. |
