# v4-home-ia — PR #44 Review Triage

Review: 6 lanes (Security, Requirements, Test Coverage, Architecture, Regression, Performance) on PR #44, 2026-07-21. Artifacts at triage time: 2 inline comments + 2 issue comments (requirements summary + consolidated summary), 0 submitted reviews. Security, Performance, Regression lanes: zero findings (Regression additionally verified the two orthogonal test fixes `34b798b`/`c0aa7c4` as legitimate tightenings and all 588 deletions as intentional).

| # | Source | Location | Finding | Verdict | Action | Rationale |
| - | ------ | -------- | ------- | ------- | ------ | --------- |
| 1 | Test Coverage inline `3623542441` | `frontend/app/components/study-heatmap.tsx:74` | `intensityLevel` levels 3 (`4..6`) and 4 (`>6`) and both upper thresholds have no covering assertion; fixtures top out at total 3 | **Real** — verified against the code: thresholds `<=1/<=3/<=6/else` exist; `study-heatmap.test.tsx` fixtures produce only levels 0/1/2, so a cutoff mutation (e.g. `<=6`→`<=60`) passes green. HOME-13's "shaded by activity count" is the gradient itself | **Fix** — add high-activity fixtures asserting `data-level="3"` (total 5) and `data-level="4"` (total 8) | Cheap, closes the one genuinely untested branch in the diff; the cycle's own Verifier mutation pass (M5) covered the densify loop but not these cutoffs |
| 2 | Architecture inline `3623544527` | `backend/app/application/study.py:123` | Bidirectional `study.py ↔ reading.py` application-layer cycle: `reading.py:26` imports `local_day` from study at module scope, forcing study's `_chapter_title` to lazily import `_chapter_of`/`locate`/`partition` — including a private (`_`) internal of its neighbor | **Real** — verified: both edges of the cycle exist; the lazy import + private reach-through were a recorded Phase A deviation (papered, not resolved) | **Fix** — move `local_day` to a neutral pure module (`application/dates.py`) and move the chapter-title resolution into `reading.py` as a public helper consumed by study. Kills the cycle AND the private reach-through; pure code motion, no behavior change | The debt is new in this PR — cheapest moment to remove it is before merge; no AD/ADR protects the cycle (AD rows pin behavior, not this module layout) |
| 3 | Requirements issue comment `5036035852` | HOME-19 ("/account reachable from the header") | No in-diff positive assertion; only a pre-existing untouched test pins it | **Real observation, wrong remedy** — the clause IS directly asserted by `frontend/tests/app-shell.test.tsx:103-105` (header Account link → `/account`), which the diff leaves untouched; the Test Coverage lane independently reached the same conclusion | **Won't-fix** — a duplicate in-diff assertion maps to no uncovered requirement and fails the necessity check (every test must trace to an uncovered criterion); spec coverage is repo-wide, not diff-scoped | Also flagged by the cycle's Verifier as non-blocking with the same disposition (`validation.md`, note 1) |

Non-finding artifacts: the consolidated summary comment (`5036071355`) and the requirements summary's verified-implemented listing carry no additional actionable claims. Lanes' "positive highlights" require no action.

Fix plan: findings 1–2 applied as two atomic commits, full backend + frontend gates re-run before push.
