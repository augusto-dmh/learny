# v3-notes-loop Review Triage (PR #43)

Review ran 2026-07-21 (fresh-context subagent → pr-review, 6 lanes). Tally: 1 inline finding, 1 requirements summary, 1 consolidation summary. Security, Architecture, Regression, and Performance lanes: zero findings after second passes. Comments are deleted in the cleanup stage; this file is the surviving record.

| # | Source | File:line | Finding | Verdict | Action | Rationale |
|---|--------|-----------|---------|---------|--------|-----------|
| 1 | inline `learny-review:tests` (id 3618723445) | `backend/app/infrastructure/web/retrieval.py:101` | Note branch of `EvidenceView` `model_serializer` (`origin`/`note_id`/`note_title`) "never asserted at the serialization boundary"; a regression dropping the note keys or leaking them into book citations would ship green | **False** | won't-fix | The claimed-missing assertions exist: `backend/tests/test_web_questions.py:864-880` (DB-backed, non-spy) asserts a note citation's **serialized JSON** carries `origin == "note"`, `note_id == str(note_id)`, `note_title == "My Insight"` AND that book citations in the same response have exactly the seven legacy keys with no note-field leak (`:871-877`); `:883-937` repeats both assertions through the SSE `data-citations` path. `AnswerResponse` reuses the same `EvidenceView` class as `/retrieve`, so the serializer branch is pinned once at the boundary that exercises it with real note evidence; duplicating the assertion on the diagnostic `/retrieve` route (which defaults `include_notes=false` and was tested with an empty-spy) would be redundant coverage of the same code path. The lane's search stopped at `test_web_retrieval.py` and the default-forwarding spy tests. |
| 2 | PR-level `learny-review:requirements` (id 5029043892) | — | All 24 requirements + ADR-0026 d4–d6 constraints traced to code; advisory: manually fact-read README + retrospective since the two doc ACs are build-gate-only | Real (advisory, no defect) | no action | Matches the Verifier's 22/24 + 2-by-design result. The doc-accuracy concern was already mitigated during the build: the release-close worker grep-verified ~25 concrete claims (endpoints, defaults, settings, migrations) against branch code before committing, and reported none unverifiable. |
| 3 | PR-level `learny-review:summary` (id 5029070704) | — | Consolidation summary, no action items | — | no action | Informational. |

**Totals:** 1 real-with-no-defect advisory, 1 false, 0 fixes to apply. Stage 5 (fixes) is a no-op; gates stand as verified (backend 1522/10 + ruff; frontend 494/50 + tsc + build; Verifier 7/7 mutants killed).
