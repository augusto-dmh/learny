# Review triage — v5-eval-dashboard (PR #61)

Nine findings from the six-lane review (Security 0, Test Coverage 4, Architecture 3, Performance 1,
Requirements 1 consolidated). Each judged against the code as it exists, not against the reviewer's
authority. **All nine are real; all nine are actioned** — one of them turned out to be worse than reported.

| # | Source | File:line | Verdict | Action | Rationale |
| --- | --- | --- | --- | --- | --- |
| F1 | inline 3697666399 (tests) | `backend/tests/test_eval_results.py:180` | **Real — and understated** | Fix | Reviewer flagged `run_index`/`error_count`/`other_count` as never non-default, warning that a regression would collide React keys. Checked the real file: `2026-07-31-c2bf2375-generation-denoise.jsonl` holds **two** generation arms (sonnet + opus) × 3 runs × 12 cases, so `case_id`+`run_index` is not unique **today** — **36 of 36 keys collide** when that run is expanded. Not a latent regression risk: a live defect on the newest real file. Fix the key and add the sensors. |
| F2 | inline 3697666712 (tests) | `backend/tests/test_eval_results.py:227` | Real | Fix | The committed-files test asserts only that parsing succeeds. A change reclassifying half the real lines would keep `len == 5` / `unparsable == 0` green. Pin the parsed *shape* per file — stable across recalibrations, unlike verdicts. |
| F3 | inline 3697667099 (tests) | `backend/tests/test_eval_results.py:359` | Real | Fix | The equivalence parametrization can only cover shapes the gate survives, so `gate_outcome`'s two documented divergences (missing `citation_valid`; answered line with a null score) and `partition_families` dropping an identity-less record are untested by construction. These branches exist only for malformed input — the input no fixture produces by accident. Three direct tests. |
| F4 | inline 3697667183 (performance) | `backend/app/infrastructure/web/evals.py:147` | Real (non-blocking) | Fix, reduced scope | The list ships every case of every run and the page renders none of them until a per-run toggle opens. Correct about the slope: nightly cron, no retention, no cap or cache anywhere in the read path. Taking the **bound** (newest-first `limit`, with the discovered total reported) rather than the recommended split into a second per-run endpoint: the bound removes the unbounded-growth property — the actual risk — without restructuring one endpoint and one component into an async drill-down, in a cycle the RFC scopes as "compact & cuttable". The per-run cases endpoint is recorded as a follow-up, not silently dropped. |
| F5 | inline 3697667484 (tests) | `frontend/tests/eval-dashboard.test.tsx:323` | Real | Fix | "Try again" is asserted to exist and never clicked, which proves nothing about whether it works; and the `catch` branch (rejected fetch / non-JSON body) has no test at all. Drive the retry with a stateful mock; add a rejected-fetch case. |
| F6 | inline 3697670171 (architecture) | `docker-compose.override.yml:42` | **Real — user-visible bug** | Fix | Verified the path arithmetic: build context is `./backend`, `WORKDIR /app`, so `judge.py` is at `/app/app/eval/judge.py`, `parents[2]` is `/`, and `RESULTS_DIR` resolves to `/evals/results` — which is not in the image, because `evals/` sits outside the backend build context. The one environment that enables the dashboard therefore renders "No eval runs found" permanently and silently, and this PR's own README points at that path. Mount the directory and set the variable in the same overlay block. |
| F7 | inline 3697670519 (architecture) | `backend/app/main.py:81` | Real | Fix | `eval_dashboard.surface.refused` is emitted on the `app.instrument` logger — the one axis operators filter on, and the one place the two deliberately-independent surfaces are still fused. The test having to encode the mismatch (`EVAL_LOGGER = "app.instrument"`, commented "dashboard") is the tell. Give each surface its own logger. |
| F8 | inline 3697670923 (architecture) | `backend/app/eval/results.py:176` | Real | Fix | `gate_outcome` re-implements `_assert_aggregates` condition for condition; only a nine-shape test keeps them equal, and the tenth shape someone adds to the gate is the one that diverges. Both live in `app/eval/`, so nothing prevents one owner. Extract the predicate into `judge.py`, have the assert and the dashboard share it, and the equivalence becomes structural instead of sampled. This strengthens the exact invariant the cycle is built on. |
| F9 | issue 5155047849 (requirements) | `frontend/app/components/eval-dashboard.tsx` | Real (latent) | Fix | The headline metrics and both trend series come from `headlineTier` (silver-if-scored, else golden, status-`ok` only), while the verdict is derived over **all** generation lines. So spec P1 AC2's "citation-valid rate over all generation lines" is served as a per-tier, status-filtered rate, and a mixed-tier or error-carrying run can show metrics above threshold beside a `fail` badge with nothing explaining it. Latent today (the nightly writes neither `tier` nor `status`) but reachable through the study path that `ab.py` exists to serve. Report run-level aggregates over the same line set the verdict uses, and render those as the headline. |
| F10 | issue 5155047849 (requirements) | `backend/app/infrastructure/web/evals.py` | Real | Fix | Every test that reaches a 200 sets `LEARNY_EVAL_RESULTS_DIR`, so the `or RESULTS_DIR` fallback is never exercised — dropping or inverting it fails nothing. The verifier's W4 mutation covered the opposite direction only. One test. |
| F11 | issue 5155047849 (requirements) | `.specs/project/STATE.md` | Real | Fix | AD-245 is the decision behind a shipped payload field and behind the marker the PR body foregrounds, and it never reached the project decision log; `context.md`'s header still claims the range ends at AD-244. The one decision a future reader is most likely to look up and not find. |
| F12 | inline 3697674545 (regression) | `backend/app/eval/results.py:207` | **Real — and it falsifies a claim I wrote** | Fix | The citation condition treats a *missing* `citation_valid` as a violation, justified in the docstring as "not producible by the writer". Verified that is true of `judge.run_eval` and **false** of the study runner: `tests/eval/study.py:226-245` writes `{"status": "error", "faithfulness": None, "relevancy": None, "case_id": …}` — no `citation_valid`, no `found` — into the same default directory, and `tests/eval/silver.py` does the same for `error`/`skipped`/`broken`. So one provider 5xx mid-study (precisely the AD-238 credit-exhaustion situation) would flip that run to `fail: citation`, the most alarming badge the page has, for lines the gate never evaluated — while the same payload showed `citation_valid_rate` 1.000 beside it, because `ab.aggregate` excludes non-`ok` lines and `gate_outcome` did not. Restrict the derivation to `status == "ok"` lines, let an all-error run read `not-evaluated`, and correct the docstring. Equivalence with the gate is preserved because `judge.run_eval` never writes `status`, so every gate-produced line is `ok` by default. This same filter closes the metrics-vs-verdict half of F9. |
| F13 | inline 3697674745 (regression) | `evals/README.md:23` | **Real — my documentation is wrong** | Fix | Same root cause as F6, and the README states it as fact: "`docker compose up` enables it locally" and "by default it reads this directory" are both false inside the container. The `git worktree` recipe is wrong twice over — a relative `../learny-eval-results` resolves against the process cwd (`/app` in the container, `backend/` under bare uvicorn), not the repo root, and a host path is invisible to the container regardless. Rewrite the section to match reality. |
| F14 | Architecture lane note (not posted) | `backend/tests/test_eval_results.py:227` | Real | Fix | `test_the_committed_result_files_all_parse` asserts `len(runs) == 5` against the live `evals/results/`, so committing a sixth result file breaks an unrelated test. Fold into F2: pin the known runs by id as a subset rather than pinning the directory's total. |

## Below-bar observations (Security lane, not posted) — reviewed, no action

- `results_dir` returns the process's absolute path and is rendered. Authenticated and non-production only, and the repo layout is already public. Agreed: negligible.
- `frontend/app/(app)/dev/evals/page.tsx` ships in the production bundle though the endpoint 404s there. Discloses only that the feature exists; the component settles to its disabled state. Agreed: negligible.

## Recorded as follow-up, not done here

- **Per-run cases endpoint** (`GET /api/dev/evals/{run_id}`) so the collapsed default costs nothing, per F4's original recommendation. The `limit` bound taken here caps the payload; the split is the further optimisation if the page ever feels heavy.

## Nothing rejected

No finding misread the code, duplicated an accepted decision, or traded against a recorded scope decision. F4 is the only one whose *remedy* was reduced, with the reasoning above and the remainder recorded rather than dropped.

## Late addition

| # | Source | File:line | Verdict | Action | Rationale |
| --- | --- | --- | --- | --- | --- |
| F15 | Regression lane nit (not posted) | `backend/tests/test_eval_results.py` | Real | Fix | The "exactly at both thresholds" equivalence case uses `relevancy=4`, so the relevancy boundary of 3.1 — the constant Cycle B moved — is never exercised at its edge. A single line cannot average 3.1 (scores are integers), but ten can: nine 3s and one 4. Add that case. |
