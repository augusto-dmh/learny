# v2-active-recall — PR #25 Review Triage

**Date:** 2026-07-17 · **Review:** 6-dimension fresh-context pr-review fleet (Security, Requirements, Test Coverage, Architecture, Regression, Performance) · 6 inline + 2 PR-level comments. Security, Requirements, and Regression posted zero findings. Comments are deleted after fixes (ship-cycle Stage 6); this file is the surviving record.

| # | Source comment | File:line | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| 1 | inline 3600359155 (architecture) | `backend/app/infrastructure/quiz/anthropic.py:37` | Quiz adapter imports module-private `_AnthropicAdapter` across the `answering/` module boundary | **Real** | **Fix** | Confirmed: leading-underscore internal subclassed from another module. Cycle C's accepted deviation covered sharing the base *within* `answering/`; cross-module reuse makes it a de-facto public seam. Fix = promote to a declared public name (`AnthropicAdapterBase`) at its current home, update both consumers — no behavior change. |
| 2 | inline 3600362933 (performance) | `backend/app/infrastructure/db/repositories.py:1008` | `list_for_source` projects `select(quiz_items)` incl. the unused 1536-dim `embedding` column; backs the 3s overview polling target + export | **Real** | **Fix** | Confirmed: `_to_quiz_item` never reads `embedding`; the overview is polled — per-item ~1536 floats of pure waste. Fix = explicit column projection excluding `embedding`. |
| 3 | inline 3600363530 (performance) | `backend/app/infrastructure/db/repositories.py:1072` | `due_for_user` likewise projects the full `quiz_items` table incl. `embedding` for up to 100 rows per request | **Real** | **Fix** | Same defect, same fix, on the `GET /api/reviews/due` path. |
| 4 | inline 3600364001 (performance) | `backend/app/application/quiz.py:256` | Finalize embeds candidates one `embed_query` call at a time inside the loop — O(N) sequential provider round-trips under the OpenAI adapter | **Real** | **Fix** | Confirmed. `EmbeddingPort.embed_documents` exists (used by EmbedCorpus). Fix = after grounding QC locates candidates, batch-embed the surviving texts in one `embed_documents` call, then run the sequential dedup comparisons over precomputed vectors. Semantics unchanged (embeddings are input-only functions; dedup order preserved). |
| 5 | inline 3600364093 (tests) | `backend/app/worker/tasks.py:472` | `poll_quiz_deck`'s `collect_deck`-raises → `_retry_or_fail_deck` branch has no test (sibling `generate_quiz_deck` has one) | **Real** | **Fix** | Confirmed uncovered branch on the retry/terminal classification path. Fix = add a poll-task provider-fault test mirroring `test_generate_provider_fault_retries_then_fails`. |
| 6 | inline 3600364140 (tests) | `backend/app/infrastructure/web/quiz.py:84` | `_export_filename` defensive branches (non-ASCII stripping, `or "deck"` fallback) untested; user's real corpus has accented Portuguese titles | **Real** | **Fix** | Confirmed: only the clean-ASCII path is asserted. Fix = two test cases (accented title → stripped name; fully non-ASCII title → `deck.apkg`). |
| 7 | PR-level 4998962433 (requirements) | — | Requirements review: all 25 ACs + ADR constraints verified met; one interpretation note — the "source changed" indication lives on the library screen, not the review card | **Real (informational)** | **Won't-fix (no defect)** | The note matches the recorded design: the due queue is active-only, so review cards always have resolving anchors; stale/orphaned surfacing on the library screen is the designed placement (validation.md records the same conclusion). No code change. |
| 8 | PR-level 4998986291 (summary) | — | Consolidation summary table | **Artifact** | **No action** | Review-process artifact, posted as a deletable issue comment per convention; removed in Stage 6. |

**Counts:** 6 code findings — 6 real, 6 fix; 0 false positives; 2 informational PR-level artifacts, no action.
**Fix plan:** one commit per concern — (a) refactor: public Anthropic adapter base; (b) perf: drop embedding column from overview/due projections; (c) perf: batch-embed finalize candidates; (d) test: poll provider-fault branch + export filename fallbacks. Gates re-run before push: full backend (pytest + ruff) and frontend suite untouched by fixes (vitest run for safety).
