# Review Triage — house-profiles (PR #71)

Triage date: 2026-09-13. Reviewer: fresh-context `pr-review` subagent (6 lanes).
Source comments: 6 inline (`pulls/71/comments`) + 2 PR-level (`issues/71/comments`).
Every finding judged against the code as it exists on `fbdf3a6c`; verdicts and
dispositions below are the surviving record (comments are deleted after fixes).

| # | Source | Location | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| 1 | inline 3998833613 (tests, critical) | `.specs/features/house-profiles/validation.md:17` | CI `backend-test`/`compose-smoke` never ran pytest: `minio/minio` pull denied on Docker Hub (repo withdrawn), so all db-gated evidence (migration 0029, repository, endpoints, turn wiring, sensors M3/M7/M8/M10) has zero execution anywhere; validation.md's own contract says a red backend-test invalidates the PASS | **Real** | **Fix** | Independently confirmed: Docker Hub `minio/minio` returns 404 repo-not-found (tag `RELEASE.2024-10-13T13-34-11Z` unpullable for everyone); `quay.io/minio/minio` serves the exact tag (checked via quay API). Fix = repoint the image source in `ci.yml` (backend-test MinIO step), `docker-compose.yml` (compose-smoke), and `docker-compose.prod.yml` (same future break on the next prod re-creation; the VPS runs a cached copy today). The fix lands on this PR's head so the required green run is this PR's own. Note: nightly eval red for 3 days on `main` is a *separate* cause — Anthropic credit balance exhausted (job log) — surfaced to the owner at the merge gate, out of cycle scope |
| 2 | inline 3998833641 (tests) | `backend/tests/test_web_user_generation.py:218` | Teach-mode turn serving has no web-level test: `_post_turn` hard-codes answer mode, so the "Ask **or** Teach" half of HP-03 is verified end-to-end only for Ask; teach-ineligible chosen lead asserted nowhere at the surface | **Real** | **Fix** | Cheap, closes a genuine spec-wording gap: add one db-gated teach-mode case mirroring the answer-mode lead test (CI-executed) |
| 3 | inline 3998833666 (security) | `backend/app/infrastructure/web/ai.py:64` | `profile_id` unbounded in the PUT body and echoed verbatim in the 422 detail — megabyte-scale reflection through responses/body logging | **Real** (hardening, not vuln — reviewer agrees; membership validation bounds persistence) | **Fix** | Bound the field at the model (`min_length=1, max_length=128`); over-long ids get the standard pydantic 422 list instead of the friendly detail; the existing unknown-id detail test path is unaffected |
| 4 | inline 3998833681 (architecture) | `backend/app/infrastructure/web/ai.py:54` | Starlette 422-status-name compat shim duplicated character-for-character from `error_handlers.py:70-76`; two copies can diverge when the Starlette migration completes | **Real** (single-definition principle) | **Fix** | Export the resolved constant once from `error_handlers.py`; import it in `ai.py` |
| 5 | inline 3998833716 (performance) | `backend/app/infrastructure/web/dependencies.py:718` | `_user_generation_chains` never evicted in production; growth tracks operator rename history (dead ids keep chains cached for process life) | **Real** (minor today; adapters lazy) | **Fix** | Cache only ids present in the declared registry (bounded by registry size); stale/unknown ids build uncached per request — rare by design and already a warning path. Unset still resolves to the default chain object itself |
| 6 | inline 3998833747 (regression) | `frontend/app/components/AccountPanel.tsx:106` | Silent `.catch` discards catalog/choice load errors — an endpoint outage is byte-identical to an intentionally empty catalog, with no console trace | **Real** (degrade-to-absent is right; invisibility is not) | **Fix** | Keep the degrade; add `console.warn` with the cause; extend the jsdom test with a console spy |
| 7 | PR-level requirements note | spec set | Selector-visibility wording tension: AD-355/D-6 say "≥1 selectable **non-default** profile", HP-12 says "at least one selectable profile"; implementation follows HP-12 (catalog non-empty); single-declared-profile deployments get a behaviorally-identical second row | **Real** (spec-internal inconsistency) | **Fix (docs)** | Resolve in favor of HP-12 with a recorded note in `context.md` D-6: the catalog contains only declared non-seed profiles, so a sole entry IS the operator default and choosing it is an accepted no-op; tightening the rule would hide a meaningful choice in mixed registries where one declared entry is not the primary |
| 8 | PR-level requirements note | `design.md` / `tasks.md` | Shipped builder dropped the sketched `explain=False` kwarg (deliberate Phase-2 deviation, recorded in validation.md, satisfies HP-04); in-PR docs no longer match the shipped signature | **Real** (docs drift) | **Fix (docs)** | Align `design.md`/`tasks.md` to the shipped signature with the deviation rationale inline |
| 9 | PR-level requirements note | `design.md` | Route module named `ai_profiles.py` in design; shipped as `web/ai.py` | **Real** (cosmetic drift) | **Fix (docs)** | Corrected in the same docs pass as #8 |
| 10 | PR-level requirements note | `.specs/project/ROADMAP.md` | Roadmap row ships as "Not started" inside the PR | **Real** observation | **Won't-fix** | Protocol: the row flips to Done on `main` post-merge (established precedent — the spoiler-safe cycle's row was updated on `main` after the merge, outside its PR); a mid-PR "Done" would be false when the PR opens |
| 11 | PR-level 5651396983 (summary) | — | Consolidation summary comment (lanes run, marker idempotence) | N/A (no actionable finding) | No action | Bookkeeping; deleted with all comments after fixes; this file is the surviving record |

Counts: **6 inline findings — 6 real, 0 false; dispositions: 6 fix, 1 won't-fix (the roadmap-row observation), 0 rejected-as-misread.** PR-level notes: 3 real doc/spec alignments (fix), 1 won't-fix (protocol), 1 no-action summary.
