# PR #58 Review Triage — v6-answer-experience

21 comments (19 inline + 2 PR-level) from the 6-lane review, 16 distinct findings (3 locations drew two lanes). Every finding checked against the code; verdicts below are the surviving record (comments get deleted per ship-cycle Stage 6). Verified inline before triage: the `a`-override does return bare anchors for non-citation hrefs; `reasoning_id` is minted once and reused across interleaved blocks; no component reads the parsed `phase`.

| # | Source (comment id) | Location | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| 1 | 3687290492 (security) | `cited-answer.tsx:64` | `components.a` override drops Streamdown's linkSafety + `rel`/`target` hardening for non-citation (model/book-authored) links | **Real** | **Fix** | Confirmed: non-citation hrefs return a bare `<a href>`. Add `rel="noreferrer noopener" target="_blank"` on the fallback anchor (matching Streamdown's fallback branch). Indirect-prompt-injection surface via uploaded books is real even with rehype-sanitize stripping `javascript:`. |
| 2 | 3687295050 (tests) | `ui_message_stream.py:105` | Reasoning-then-decline `reasoning-end` (post-loop) and mid-reasoning error paths have no backend wire test | **Real** | **Fix** (tests) | Both branches exist and are reachable (application layer provably produces reasoning-then-sentinel); frame order is only pinned by a hand-written frontend fixture. Add the two backend tests. |
| 3 | 3687295984 + 3687308457 (tests+regression) | `anthropic.py:174/178` | Marked `answer_text` is replayed verbatim as history, so the model sees `[^n]` and can imitate one; an in-range imitation gets linked by the frontend to a citation the model never made | **Real** | **Fix** | Strip `[^n]`-shaped tokens in `_build_history_messages` (single choke point; the model never sees markers, so imitation drops to the base rate of book prose containing the token). Add the missing model-authored-token test documenting pass-through. Residual risk (book text containing a literal in-range `[^n]`) accepted — the frontend already renders out-of-range tokens as plain text, and full scrub-of-model-text would corrupt legitimate quoted footnotes. |
| 4 | 3687296835 (architecture) | `ui_message_stream.py:67` | One `reasoning_id` reused across interleaved thinking blocks → `reasoning-start` reopens a closed part id | **Real** | **Fix** | Confirmed in code; adaptive thinking is interleaved by design. Mint a fresh id per reasoning block. |
| 5 | 3687296905 (tests) | `toc-panel.tsx:171` | ChapterNav's two `LinkPendingIndicator`s deletable with suite green | **Real** | **Fix** (test) | Reuse the `app-sidebar.test.tsx` `useLinkStatus` mock pattern. |
| 6 | 3687296959 (tests) | `teach-panel.tsx:421` | Teach's `reasoning && !notFound` gate unguarded (Ask-only sensor) | **Real** | **Fix** (test) | One teach-side not-found-collapse case. |
| 7 | 3687297209 + 3687299211 (architecture+regression) | `conversations.py:585` | Retrieval failures inside the stream are wrapped unlogged — outages and programming errors (TypeError etc.) both vanish into a generic error frame | **Real** | **Fix** | Keep the wide catch (mid-stream fail-closed is correct; headers already sent), add `logger.exception` with conversation/source context before raising. Answers both lanes: outages become loud again and programming errors keep their traceback. |
| 8 | 3687297252 (architecture) | `anthropic.py:148` | `[^n]` literal is a Learny-owned cross-boundary format defined inside one provider adapter | **Real** | **Fix** | Hoist a `citation_marker(n)` helper/constant to `domain/entities.py` beside `SENTINEL` (same "Learny-owned contract" home); adapter and tests use it. Frontend regex necessarily stays an independent counterpart (different language). |
| 9 | 3687297291 (architecture) | `nav-pending.tsx:26` | App-authored, `next/link`-coupled primitives placed in the vendored-shadcn `components/ui/` directory | **Real** | **Fix** | Move to `frontend/app/components/nav-pending.tsx` where Learny-owned components live; pure file move + imports. |
| 10 | 3687297328 (architecture) | `test_answering_factory.py:50` | Factory test asserts `adapter._max_tokens`/`_effort` private attrs | Real (style) | **Won't-fix** | The factory test's one job is settings→constructor wiring; the private peek is the narrowest way to pin it. The alternatives (public accessors for tests' sake, or duplicating the adapter suite's request-capture rig in a factory test) add more surface than they remove. Contained, deliberate. |
| 11 | 3687297486 (architecture) | `anthropic.py:56` | `thinking`/`effort` sent for any configured model while `generation_model` is an unconstrained string (e.g. Haiku 4.5 rejects `effort`) | Real (latent misconfig) | **Fix** (docs only) | A model allowlist would rot as models release, and ADR-0020 already locks the family; a misconfigured model fails loudly at first request. Add one `.env.example` note: the generation model must support adaptive thinking + effort (Sonnet/Opus 4.6+). |
| 12 | 3687297602 (performance) | `anthropic.py:417` | Buffered `generate` holds a threadpool slot with no per-call timeout; thinking + 4096 budget ~4× worst-case wall clock (SDK default timeout 10 min) | **Real** | **Fix** | Bound the buffered call with a module-level timeout constant (120s — generous for medium-effort thinking + 4096 tokens), mirroring the quiz adapter's established `_SUGGEST_TIMEOUT_S` pattern. Streaming path unaffected (frames flow immediately). |
| 13 | 3687297898 (performance) | `ask-panel.tsx:366` | No `experimental_throttle` on `useChat`: reasoning deltas + 4× token budget multiply an O(n) per-frame re-render | **Real** | **Fix** | Add `experimental_throttle: 50` in `use-conversation-thread.ts`. |
| 14 | 3687298397 (tests) | `config.py:213` | Thinking-induced `max_tokens` truncation persists as a complete-looking answer | Real (residual) | **Won't-fix** | The signal exists: `_log_call` records `stop_reason` per call, so truncation is observable operationally. The 4096 default is sized for medium effort; user-facing truncation handling (retry/continuation/badge) is a product decision outside this cycle's spec, and `_parse_message`'s never-raise-on-max_tokens contract predates the cycle. Noted for the RFC-005 resumption backlog. |
| 15 | 3687309292 (regression) | `streaming.ts:79` | `assistantView().phase` (and the `data-phase` part type) parsed but never read by any component | **Real** (dead code) | **Fix** | Confirmed: `AnswerPhaseIndicator` takes no props; panels gate on `pending && !text && !reasoning`, which satisfies the spec's searching-state AC without reading the frame. Remove the dead `phase` field from `assistantView` (+tests); keep the wire frame itself — it is the spec-mandated protocol signal, backend-tested, and the extension point the part type exists for. |
| 16 | 3687310007 (regression) | `anthropic.py:56` | Code comment "provider default omits the content" allegedly backwards per SDK docstring | **False** | **Reject** | The comment describes the *server-side* default for the current model family, which is `omitted` — exactly what the dogfood session observed empirically (empty thinking blocks streaming with `display` unset, recorded as this cycle's originating finding) and what current provider documentation states for Sonnet 5 (a silent change from the 4.6 generation, where the default was `summarized`). The pinned SDK's field docstring predates that flip; a stale client docstring does not outrank observed live behavior. No change. |
| 17 | 3687310803 (regression) | `citations.tsx:118` | Escape-to-dismiss, outside-click/focus-return, and aria wiring lost in the popover→in-flow-div move | **Real** | **Fix** | Add Escape handling that closes the passage and returns focus to the invoking chip/mark, plus `aria-expanded`/`aria-controls` on the chips. Outside-click dismissal is deliberately NOT restored: an in-flow region (not an overlay) reads as page content, and closing it on any stray click would fight text selection/scrolling; Escape + explicit close covers keyboard users. |

**Tallies:** 16 findings — 14 real (12 code/test fixes + 1 docs-only fix + counting 3/7 merged pairs once), 2 real-but-won't-fix (10, 14), 1 false (16). PR-level comments: requirements review (its one ❌ — the stale spec.md traceability table — is real; fixed alongside as a docs commit) and the review summary (no findings of its own).

**Also from the requirements review:** spec.md traceability table still shows Pending/unmapped while validation.md records Verified → fix (docs); the 150ms vs ~120ms delay note is a non-issue (the design said "~120ms", 150 is within "~", and the delay is what tests pin); the mid-stream-error-inside-200 note is the recorded consequence of AD-221, not a defect.
