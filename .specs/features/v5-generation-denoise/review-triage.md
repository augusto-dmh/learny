# PR #60 review triage — v5-generation-denoise (2026-08-01)

Reviewer: fresh-context pr-review (6 lanes). 10 inline + 2 PR-level comments, 8 distinct findings after dedupe. No Security/Critical findings. Comments deleted after triage per ship-cycle contract; this file is the surviving record.

| # | Comment id(s) | File:line | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| F1 | 3697319589 + 3697323907 (dupe pair) | tests/eval/study.py:240 | Fresh-scored `skipped`/`broken` units bill full unit cost while the resume meter (`_BILLED_STATUSES`) bills them nothing — first invocation and resume disagree on modeled spend; several skipped silver books could fire a false budget stop | REAL | FIX | Genuine asymmetry: in-loop increment is unconditional, resume sum is status-filtered. Bill in-loop on the same `_BILLED_STATUSES` predicate. The pre-unit budget check stays conservative (status unknown before scoring) — that is intended. |
| F2 | 3697323618 | tests/eval/study.py:157 | No test pins `error` as a billed status — mutating `_BILLED_STATUSES` to `{"ok"}` survives the suite; this is the arithmetic the spend report and any real resume rely on | REAL | FIX | Verified: existing tests cover only the ok-billing and skipped/broken-zero-billing halves. Add the mirror test (recorded error line consumes ceiling exactly once). |
| F3 | 3697323983 | tests/eval/study.py:138 | `load_recorded` aborts with a raw `JSONDecodeError` on a truncated final line or blank line — the exact artifact a hard kill mid-append produces, on the path that exists to survive it | REAL | FIX | Chosen semantics: skip unparsable/blank lines (a truncated tail means that unit's result was lost mid-write, so re-attempting it is correct — matching the checkpoint promise), pinned by test. |
| F4 | 3697324313 | tests/eval/study.py:199 | `StudyReport.paths` is written and never read — dead code | REAL | FIX | Confirmed via grep: assignment only. Drop the field, the assignment, and the then-unused `field` import. |
| F5 | 3697324417 | tests/eval/test_generation_study.py:110 | Judge→`SilverJudgement` adapter duplicated near-verbatim from `test_silver.py`; the copy that runs under spend has no offline coverage | REAL | FIX | Move a duck-typed `judge_adapter(judge, prompt_hash_value)` into `tests/eval/silver.py` (owner of the contract; no provider import needed) and wire both entrypoints through it. |
| F6 | 3697325679 + 3697326556 (dupe pair) | tests/eval/test_generation_study.py:145 | The fixed-evidence memoization (a pinned study decision) lives in a live-only closure — unreachable by any offline test; a regression would silently turn the A/B into a retrieval comparison | REAL | FIX | Lift `memoize_retrieval(retrieve)` into `tests/eval/study.py`; cover with a counting fake (one underlying retrieval per case across arms × runs). Entrypoint keeps wiring only. |
| F7 | 3697325060 | .specs/…/design.md:47 | Design doc says "No `live`/`eval` markers" and "module skip"; shipped entrypoint carries `live` (guard test asserts it) and skips via fixture | REAL | FIX | Record-accuracy only (nightly needs both markers, so behavior is safe). Update the design bullet to the shipped contract. |
| F8 | 3697326090 | tests/eval/test_generation_study.py:44 | `_GENERATION_USD_PER_UNIT` keys have no offline tie to `ARMS` — a typo or third arm raises `KeyError` on the first paid unit and makes recorded-artifact resumes unrunnable | REAL | FIX | Add the offline pin `set(_GENERATION_USD_PER_UNIT) == set(ARMS)` beside the judge-pin test. The suggested negative-leg `KeyError` test is skipped — it asserts dict behavior, not a study contract. |

PR-level comments (requirements review + consolidated summary): informational, no separate findings; deleted with the rest.

Summary: 8/8 real, 8 fix, 0 won't-fix. Overall the review confirmed the pure/runner/entrypoint split and found no correctness issue in the verdict path; the substantive findings cluster on the budget meter's status handling and on live-only logic that belonged in the testable runner.
