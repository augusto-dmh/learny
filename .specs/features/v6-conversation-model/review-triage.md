# Review Triage — `v6-conversation-model` (PR #52)

22 review artifacts: 20 inline findings (security 1, performance 3, tests 6, regression 5, architecture 5), the requirements issue comment (3 gaps + DoD), and the summary. Verdicts judged against the branch at `2375e2a7`. Comments are deleted in the cleanup stage; this file is the surviving record.

| # | Comment | file:line | Finding | Verdict | Action | Rationale |
|---|---|---|---|---|---|---|
| F1 | 3651215148 (security) | web/conversations.py:115 | `scope_anchors` unbounded (list + string length), duplicates kept, per-anchor query amplification at start and on every turn, persisted | REAL | **FIX** | Every sibling field is bounded; cost is attacker-chosen and durable. Add `conversation_scope_max_anchors` + per-anchor length bound via settings, order-preserving dedupe in `StartConversation`. |
| F2 | 3651218246 (perf) | application/conversations.py:573 | Full corpus structure loaded per turn even for whole-book answer turns; regression vs pre-cycle ask (2 extra queries + TOC materialization per `/questions` call) | REAL | **FIX** | `by_anchor` is only consumed by teach dispatch and non-empty scopes; make the load conditional exactly as recommended. |
| F3 | 3651218408 (perf) | application/conversations.py:649 | `resolve_section` in a loop → one `expand_anchors` query per aliased anchor per turn; same shape in `_resolve_head` | REAL | **FIX** | The port is batch-shaped; collect misses, one expansion call, in both places. The miss path is the AD-085 post-re-ingest case — the worst case is the designed-for case. |
| F4 | 3651218458 (perf) | db/repositories.py:918 | `list_for_user` unbounded (no LIMIT/pagination); no index for `updated_at DESC` ordering or the `target_anchor IS NOT NULL` filter | REAL, split | **FIX index / WON'T-FIX pagination** | Index `(updated_at DESC, id DESC)` added to 0017 (unmerged, still amendable in-PR). Pagination is an API-shape decision owned by Cycle D's dock/list UI; single-user scale (one row per question) does not force it early. Recorded as a Cycle D obligation. |
| F5 | 3651221244 (tests) | application/qa.py:167 | Streamed-ask persistence has three negative sensors and no positive one; `answered = True` unpinned (comment it out → suite green) | REAL | **FIX** | Genuine sensor gap on the exact path the shipped panel uses; add the positive twins at service and HTTP layers. |
| F6 | 3651221262 (tests) | application/teaching.py:169 | Legacy teach turn on a target-less (ask-created) conversation is unpinned and answers 409 with wrong wording, while read says 404 | REAL | **FIX** | The legacy surface's contract is "target-less conversations do not exist" (read 404, list filtered). Make the legacy turn adapter consistent: 404 `ConversationNotFound`, pinned at both layers. Unified-surface 409 semantics (I-CM-7) unchanged. |
| F7 | 3651221444 (tests) | tests/test_web_conversations.py:1383 | `/turns/stream` is the only route without a cross-user 404 case | REAL | **FIX** | Add the intruder flow mirroring the buffered turn test (I-CM-6 sensor parity). |
| F8 | 3651221460 (tests) | application/teaching.py:60 | Legacy teaching `detail` strings never asserted end to end; `_teaching_wording` deletable with suite green | REAL | **FIX** | Tighten existing teaching router tests to full-body equality, matching the questions suite's style. |
| F9 | 3651221489 (tests) | tests/test_application_qa.py:541 | `FakeAnswerGeneration.history_calls` recorded, never asserted; "ask never continues a conversation" unpinned | REAL | **FIX** | Assert empty history per ask + fresh conversation per repeat ask. |
| F10 | 3651221517 (tests) | web/error_handlers.py:88 | `InvalidConversationTitle` missing from the error-mapping table test | REAL | **FIX** | Cheap pin; add to the enumerated test (together with the new mode error from F20). |
| F11 | 3651222384 (regression) | application/conversations.py:761 | Completion log drops `source_id`, which both predecessor logs carried | REAL | **FIX** | `plan.conversation.source_id` is in hand; restore the field and re-assert it in the two completion-log tests. |
| F12 | 3651222569 (regression) | core/config.py:173 | Deprecated knobs validate but do nothing; a deployed override silently reverts; the "keeps env validating" rationale is void under `extra="ignore"` | REAL | **FIX (warn)** | One startup warning naming each deprecated var that is set, plus an honest comment. Inheriting old→new values is rejected: it re-couples the knobs AD-198 separated, for a deployment (this one) that never tuned them. Amends AD-198's stated rationale. |
| F13 | 3651222613 (regression) | web/rate_limit.py:161 | Docstring promises one surface budget, but `_client_key` buckets per concrete path — per-conversation turn budgets defeat CONV-22 | REAL | **FIX** | Key `rate_limit_conversations` on the route template (turn budgets shared across conversations), docstring states the real guarantee (one limit value per route template). `_client_key` and legacy limiters untouched (AD-199: shipped 429 behavior stays). |
| F14 | 3651222637 (regression) | db/metadata.py:342 | Target trio all-or-nothing by convention only; DB allows partial rows that would 500 the legacy list path | REAL | **FIX** | `ck_conversations_target_all_or_nothing` CHECK in metadata + 0017 (unmerged); backfill already satisfies it. |
| F15 | 3651222671 (regression) | application/qa.py:184 | `suppress(Exception)` hides a failed discard — orphan conversation with no trace | REAL | **FIX** | Replace with try/except + one content-free `logger.warning`. |
| F16 | 3651645357 (arch) | domain/ports.py:636 | Legacy-only read (`list_for_source_with_target`) widens the domain port; the rule also lives a second time in `ReadTeachingSession` | REAL, split | **WON'T-FIX move / FIX cross-cite** | The SQL query must keep the legacy `created_at DESC` wire ordering and stays one deletable unit with its impl at retirement; relocating the filter to Python trades a frozen-wire guarantee for layering purity. Reviewer's own fallback applies: keep SQL, make the two homes cite each other so the rule has one stated owner. |
| F17 | 3651645385 (arch) | domain/ports.py:576 | Two generation ports never converged; service pays branch/rename/union costs; ask path wired to a teaching port it can't reach | REAL (follow-up) | **WON'T-FIX now / FIX record** | Reviewer states "no change needed in this PR". Convergence lands with endpoint retirement — added to ADR-0029's retirement plan so it is not forgotten. |
| F18 | 3651645402 (arch) | web/teaching.py:129 | `TargetView` derefs three nullable fields guarded only by convention in two other layers | REAL | **FIX** | Add `Conversation.teach_target` optional-trio accessor in the domain; `TargetView`/legacy paths consume it. With F14's CHECK, the invariant is enforced at DB and type levels. |
| F19 | 3651645526 (arch) | application/teaching.py:47 | Wire wording authored in application layer while the sibling status collapse sits in `web/legacy_status.py` | REAL, split | **WON'T-FIX move / FIX docstrings** | The wording rides the error *type* through the global handler; moving it web-side needs per-router exception wrapping — more machinery for the same deletable shim. Reviewer's fallback: state the placement rule in the module docstrings of both halves. Both files die together at retirement. |
| F20 | 3651645536 (arch) | application/conversations.py:560 | Unknown-mode guard raises `ValueError` (500) outside the published error vocabulary | REAL | **FIX** | Add `InvalidConversationMode` → 422 beside `InvalidConversationTitle`; mapping-table test extended (with F10). |
| R1 | 5080920561 (reqs) | .specs spec.md | Traceability table still Pending, goals/success criteria unticked despite Verifier PASS | REAL | **FIX** | Flip to Verified per the previous cycle's convention. |
| R2 | 5080920561 (reqs) | README.md:199 | API surface table missing `/api/conversations` | REAL | **FIX** | Add the resource row(s); legacy rows stay (they still exist). |
| R3 | 5080920561 (reqs) | docs/ops/rollback.md:73 | Runbook promises reversible-or-forward-only; 0017 is reversible **and** lossy (deletes every persisted ask) | REAL | **FIX** | Add the lossy-downgrade caveat where the operator looks, naming 0017. |
| R4 | 5080920561 (reqs DoD) | spec edge case | No sensor for rename/delete racing a turn post | REAL, marginal | **WON'T-FIX** | The interleaving is decided by Postgres (FK cascade + unique arbiter), both independently sensored; there is no application branch for a test to discriminate. Recorded as accepted residual. |
| R5 | 5080920561 note + F1 aside | backend/.env.example | New `conversation_*` knobs (and F1's scope caps) undocumented in `.env.example` | REAL | **FIX** | Rides F1's settings commit; the instrument cycle set the precedent of documenting new settings sections. |

**Totals:** 25 findings triaged — 25 real (0 false), 20 fix (3 of them partial/split), 5 won't-fix halves with recorded rationale (pagination → Cycle D, port relocation, wording move, port convergence → retirement, race sensor).

**Fix plan (one Opus worker, atomic commits):** (1) scope bounds + dedupe + `.env.example` [F1,R5]; (2) lazy structure + batched expansion [F2,F3]; (3) 0017 amendment: ordering index + target CHECK + metadata + migration-test extension [F4,F14]; (4) log `source_id` + discard warning + `InvalidConversationMode` [F11,F15,F20]; (5) route-template key for `rate_limit_conversations` [F13]; (6) legacy target-less turn → 404 + `teach_target` accessor + cross-cites/docstring rules [F6,F16,F18,F19]; (7) deprecated-knob startup warning [F12]; (8) test additions/tightenings [F5,F7,F8,F9,F10]; (9) docs: README + rollback + ADR-0029 retirement addendum [R2,R3,F17]; (10) specs: traceability flip [R1]. Full gates before push.
